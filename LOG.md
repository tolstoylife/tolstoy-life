# Tolstoy Research Platform — Work log

Append-only, dated, short. Project-level decisions and context — "what happened, and why." Not "what code changed" (git already does that).

For content-level operations (source ingestions, wiki queries, lint passes), see `website/src/sources/log.md`.

---

## 2026-04-22

- Adopted the shared project template (`/Users/johanedlund/Projects/PROJECT-TEMPLATE.md`). Added LOG.md at the root and moved TODO.md from `_generated/project/TODO.md` to the project root. README.md and CLAUDE.md were already there.

## 2026-04-23

- Completed PWA architecture review (TODO priority 5). Produced `_generated/PWA/architecture-review.html` — a single-column serif reading artefact in ten parts: sync-model explainer, storage topology, install-UX (with CSS-only device mockups), sync-visibility UX, volume/bandwidth risk analysis, CRDT/QR sync problems, a build-pipeline cascade bug, hosting strategy, build-minute economics, and a ranked priority-fix list. Synthesised from five parallel research investigations run during the session.
- Committed to the **Cloudflare consolidation arc**: CF Pages as parallel deploy target now (primary later via DNS flip), CF Workers + Durable Objects for the Stage 4 sync relay (Paid tier required, $5/mo floor — the "free tier" claim in `yjs-schema-and-sync.md` §7 is aspirational), CF Registrar for the domain at next renewal (~$22/yr saved vs Netlify's retail $50), R2 for LightRAG + sync-relay backups. Netlify kept as passive standby; legacy free plan confirmed grandfathered at 100 GB/month indefinitely ("no action required" per Netlify's own docs).
- Identified a critical pipeline issue: the `wikiPreviewsUrl` field embedded in every per-work manifest means any wiki edit re-versions every work, collapsing the 3-version retention discipline. Must fix before Stage 1 ships. Plus `git_first_commit_date_for_dir` broken on Netlify's shallow clones; the §2.3/§8 spec conflict for annotation body shape will cause silent data loss at Stage 4. Full findings and action items in `_generated/PWA/handoff-2026-04-23.md`.
- Corrections to the earlier PWA docs where research contradicted them: BarcodeDetector is NOT in iOS Safari (BIP-39 must be primary iOS path, not fallback); Vercel is ruled out (Image Opt default-on breaks SHA-256 determinism); CF Pages has a 20,000-file / 25 MB-per-file cap that bites at Phase 5 (hence the Pages+R2 split is load-bearing, not optional).

## 2026-04-24

- Reorganised TODO.md priority 5 (PWA follow-up) into five subsections — A. Stage-1 blockers, B. UX components to build, C. Spec propagation, D. Infrastructure decisions, E. Stage-4 fixes — and added items that were in the handoff but not yet in the backlog: the install-UX web component, the sync-visibility glyph indicator, iOS install-before-download wiring, spec propagation for findings #8–#14, `yjs-schema-and-sync.md` §6.2 correction, `relatedWiki` shape alignment between `wiki-integration.md` §2.2 and `tl-pipeline-integration.md` §4.3, Phase-5 CF Pages + R2 architecture note, the two-page sync security spec, and the Hocuspocus-on-Fly fallback Dockerfile.
- **Fixed task A1 — the wiki-previews/manifest cascade bug** (the top-ranked Stage-1 blocker from the 2026-04-23 review). Edited `_generated/PWA/tl-pipeline-integration.md`: removed `wikiPreviewsUrl` from the per-work manifest schema (§4.3) and field notes (§4.4); tightened §3.2 to state that the work hash is computed over rendered content files only, explicitly excluding generated metadata and cross-references; added a new §4.6 "Cross-reference isolation (the no-cascade rule)" with two enforcement rules (hash-time walk exclusion, and no wiki-bundle pointers in per-work artefacts); updated the §6.2 Python sketch to filter `manifest.json` / `_related-wiki.json` from the hash input and swapped the broken `git_first_commit_date_for_dir` call for a `resolve_content_date` placeholder referencing the pending stateful-`contentDate` fix (task A5). The wiki-previews bundle URL now lives only at the top of `/works.json`; wiki edits change that one file and the bundle, but no work's hash, manifest, or versioned subtree. The 3-version retention discipline is restored. Follow-up: `wiki-integration.md` §2.2 still carries the nested `relatedWiki` shape and needs to be aligned with the flat-slug-array shape now canonical in §4.3 (tracked as task C3 in TODO).
- **Fixed task A2 — the §2.3 / §8 contradiction in `yjs-schema-and-sync.md`** (the second-ranked Stage-1 blocker; a Stage-4 data-integrity risk that must be resolved before sync coding begins). The two sections disagreed on the shape of an annotation's `body`: the schema section (§2.3) specified a Y.Array of plain objects; the decision log (§8 item 1) specified Y.Text. With plain objects, a concurrent "edit this note on two devices" scenario produces silent duplicates (`body.delete(0)` + `body.insert(0, newObj)` merges into a two-entry array with no conflict signal). Resolved in favour of §8's Y.Text decision — the shape with proper CRDT semantics. §2.3 completely rewritten to walk through the failure mode step by step, explain why Y.Map body items + Y.Text value fix it, and spell out the trade-off (visible interleaved text beats phantom duplicates for data integrity). Added a `createTextualBody` factory function to hide the construction and called out a concurrent-edit regression test fixture as required from the annotation-layer package's first commit — so a future refactor can't silently reintroduce the broken shape. §2.2's example code updated to use the new shape. §8 item 1 wording tightened to point at §2.3. JSON-LD wire format is unchanged: Y.Text projects to plain string on export, Y.Map to plain object.
- **Fixed task A3 — wired in the deterministic-build CI check** (the third-ranked Stage-1 blocker). Created `website/.github/scripts/check-determinism.mjs` — a 143-line Node harness that runs `npm run build` twice, walks `dist/`, computes SHA-256 of every file, and diffs the two hash maps. Prints differing paths, short hash prefixes, and a pointer at the common non-determinism sources (timestamps, walk order, JSON key ordering, locale-dependent sort, random IDs) on failure. Exits non-zero on any mismatch. Wired to `website/.github/workflows/determinism.yml` — runs on every PR to `main`, every push to `main`, and via `workflow_dispatch` for ad-hoc manual runs. Node 22, same style as the existing `build.yml` / `validate.yml`. Promoted the bullet mention in `tl-pipeline-integration.md` §6.3 into a new §6.4 documenting the harness, its scope (Eleventy output today; the Layer-1 generators land into the same `dist/` and inherit coverage automatically), and its limitation (within-environment only — cross-host determinism between Netlify and CF Pages is a separate test that'll be wired alongside the parallel deploy). **Note before enforcing:** run `node .github/scripts/check-determinism.mjs` locally once before marking this workflow a *required* status check in branch protection — if the current Eleventy build has latent non-determinism, that'll show up as a failure on the first run and any fix should land before the check starts blocking PRs.
- **Ran the determinism check locally to audit the current Eleventy build.** Two files drift between back-to-back builds: `serviceworker.js` (`CACHE_NAME = 'cache-{buildTime}'` — deliberate timestamp cache-buster) and `feed.xml` (Atom `<updated>` field falls through to `new Date()` because `collections.posts` is currently empty). Both are shelved until their natural replacement moments rather than papering over them now: Workbox will replace the hand-written service worker in Stage 1 (and supplies content-hashed cache names out of the box), and the feed's `<updated>` will stabilise once the first post is written or the project decides against a blog section (either decision supplies a meaningful fallback date). Applied one partial fix in `src/common/feed-atom.njk`: moved `{% set postslist = collections.posts %}` above the `<feed>` element where the filter first references it — a genuine correctness bug (variable used before definition) that would have surfaced silently once posts existed. This alone is not enough to make `feed.xml` deterministic today (empty collection still trips the fallback), but it's the right shape for when posts arrive. Workflow posture: installed-but-not-enforced. Runs on every PR as a visible red/green signal, does not block merges. Flip to required-status after both drifts resolve. TODO A3 entry updated with the full reasoning so the posture is explicit for future sessions.
- **Fixed task A4 — chapterUri validator** (the fourth-ranked Stage-1 blocker), at substantially smaller scope than originally planned. A `find` of `src/works/**/text/*.md` returned zero matches: no work has a `text/` subfolder yet, so there are no chapter files in the corpus to migrate. That means A4 reduces from "migration script + validator + new workflow" to a single surgical augmentation of the existing validator. Extended `website/.github/scripts/validate-frontmatter.mjs`: the pre-existing skip-chapter-files branch (line 170) now routes chapter files to a new `validateChapterFile()` function instead. The function checks presence, format (`urn:tolstoy-life:<work-slug>:<chapter-id>` with both kebab-case), work-slug agreement with the frontmatter `work` field (catches copy-paste-forgot-to-update errors), and global uniqueness across the corpus (catches duplication, which would make annotations anchored at that URI ambiguous). The existing `validate.yml` workflow already runs the validator on every PR that touches `src/works/**/*.md`, so no new workflow file is needed. Smoke-tested by creating five temporary chapter files in `anna-karenina/text/` covering each failure mode — missing field, malformed URN, duplicate URI, work-slug mismatch — and confirmed each produces a specific, file-named error message citing §8.1; removed the fixtures; confirmed a clean run on the real corpus (29 files, zero errors). Promoted the coverage note to `tl-pipeline-integration.md` §8.2. When Phase 5 TEI ingestion begins, chapter files must carry `chapterUri` from file one (the validator catches any miss on the PR, not after); a bulk-migration script can be added alongside that pipeline only if a batch of URI-less files ever actually lands.
- **Paused on A5 and A6 — both deferred until the Layer-1 pipeline exists.** A5 (replace `git_first_commit_date_for_dir` with stateful `contentDate`) and A6 (adopt rfc8785 canonical JSON + NFC string normalisation) are both fixes to generator scripts that haven't been written yet. Same pattern as A4's migration: you can't fix code that doesn't exist. Spec direction is recorded — the §6.2 implementation sketch already uses a `resolve_content_date` placeholder that calls out A5 by reference, and A6's canonical-JSON helper is named as a shared dependency of the four Layer-1 generators (`generate-wiki-previews.py`, `generate-related-wiki.py`, `generate-asset-manifests.py`, `generate-works-index.py`). TODO entries updated to make the dependency explicit. This is the natural end of the pre-pipeline pass on Stage-1 blockers (subsection A): four of six are resolved (A1–A4), two wait for the pipeline to exist (A5–A6). The next actionable Stage-1 work lives in subsection B (UX components: the install-UX web component, the sync-visibility glyph indicator) or subsection C (spec propagation: aligning `wiki-integration.md` §2.2 with `tl-pipeline-integration.md` §4.3 on the `relatedWiki` shape; propagating findings #8–#14 from the architecture review back into the five design documents).

## 2026-04-25

- **Reorganised the project documentation surface** along the lines of `~/Projects/PROJECT-TEMPLATE.md` (AGENTS.md/CLAUDE.md split). The previous 732-line `CLAUDE.md` had grown into a single doc carrying mission, architecture, schema, project structure, PWA contracts, ebook toolset details, contribution model, and phase plan — too dense to function as scannable operating guidance. Split into three files: `AGENTS.md` (265 lines) is the tool-agnostic source of truth (mission, three-layer architecture, data flow, wiki ops, scaled processing model, vocabulary, schema rules with pointers to `website/schema/`, project structure, contribution model, GitHub repos), readable by any AI tool and scannable in one pass. `CLAUDE.md` (14 lines) is now a thin Claude-specific overlay pointing at AGENTS.md plus direct-write paths and skill triggers. `ROADMAP.md` (35 lines) hoists the phase-by-phase plan out of CLAUDE.md so it can move faster than the rest.
- **Hoisted operational documentation out of `_generated/` into a new tracked `docs/` tree.** `_generated/` is gitignored on purpose — it's local-only scratch for session handoffs, rendered HTML, drafts, decks. But CLAUDE.md was pointing at PWA specs, architecture deep-dives, editorial principles, and capacity planning *inside* `_generated/` — files no fresh clone would have. That was a bug, not a feature: an open-source contributor or AI assistant reading the public repo would hit broken pointers. Resolved by introducing `docs/architecture/`, `docs/pwa/`, `docs/editorial/` and moving 14 files (PWA local-first architecture, wiki integration, stage-1 implementation, tl-pipeline integration, yjs schema and sync, three ultraplan briefs, README index; architecture's internal-operations capacity planning, scalability deep-dive + report, wiki-rewrite workflow; LightRAG perf report from `lightrag/`; editorial `PRINCIPLES.md`). Cross-references inside the moved docs updated to relative siblings. AGENTS.md project tree, GitHub-repos description, and PWA pointer fixed accordingly. `_generated/` keeps the genuinely local stuff: `handoff-2026-04-23.md`, `architecture-review.html`, the rendered PWA deck, and outreach email templates (which Johan opted to keep private). The `pwa-docs-for-notebooklm.md` derivative was deleted.
- **Adopted "open project" as an explicit policy** for the public/private split. Cost projections, hardware specs, capacity numbers, alternatives-considered tables — none of this is commercially sensitive (the project earns nothing), and all of it helps anyone running similar projects. So "is this commercially sensitive" is no longer a useful filter for repo visibility; instead use "is this ephemeral session output / personal scratch" vs "is this an operational fact a contributor or AI assistant would benefit from." The internal-operations doc lost its "Not tracked in git" warning header and moved to `docs/architecture/`. The `2026-04-25` changelog entry on tolstoy.life records the public-facing piece (operational docs are now publicly browsable).
- **LightRAG: switched embedding model to `bge-m3` (1024d)** for stronger multilingual/Russian retrieval ahead of the TEI ingestion phase. Previous `nomic-embed-text` (768d) is adequate for English but weak on Russian. Added `fastapi`, `uvicorn`, and `pyjwt` to `requirements.txt` for the local query server (`server.py` on port 8420). Tracked the two helper scripts (`diagnose.sh`, `start-ui.sh`) as peers to the existing Python files. Added `*.log` to `lightrag/.gitignore` (runtime artefacts) and `/lightrag.log` to the root `.gitignore` (an orphan log at project root).
- **Discovered website was never registered as a real submodule — and fixed it.** CLAUDE.md and AGENTS.md described `website/` as a submodule of the parent, but `.gitmodules` only registered `tools/` and `.gitignore` line 2 explicitly excluded `website/`. Longstanding doc-vs-config drift: at some point `website/` was extracted into its own GitHub repo, but the `git submodule add` step was never run. Fixed by removing `website/` from `.gitignore` and running `git submodule add git@github.com:tolstoylife/website.git website` from the parent. Since `website/` already contained a valid clone of the right repo, `git submodule add` registered the existing checkout in place rather than re-cloning — no data loss, no re-fetch. Followed up with `git submodule absorbgitdirs website` to relocate `website/.git` into `<parent>/.git/modules/website/` (modern submodule layout), and switched `website`'s local origin from HTTPS to SSH (consistent with the parent — also avoids the credential-prompt issue we hit during a non-interactive push earlier in the session). Effects: parent now tracks a SHA pointer to the website tip; `git clone --recurse-submodules` of the parent now actually gets website; AGENTS.md's project tree and GitHub-repos section are accurate as written for the first time in a while.
- **Splash conversion deferred.** The `tolstoylife/splash` repo exists on GitHub, but the local `splash/` directory isn't a clone of it — it's a workspace containing multiple starter-framework candidates (`astro-starlight/`, `eleventy-excellent/`, `vitepress/`, `tl/`, `_netlify/`, `_originals/`, `_resources/`, `_backup/`). Looks like an evaluation directory where Johan has been comparing splash-page frameworks. Conversion to a submodule needs a separate decision about which starter wins; left alone for now, parent `.gitignore` continues to exclude `splash/`.

## 2026-04-26

- **Established the project's voice rule** — all text on tolstoy.life (wiki, works, docs prose) should be simple, factual, with as little editorial as possible; the canonical voice example is `architecture-review.html`; when editorial language is unavoidable, ask Johan for the judgment rather than guess. Saved to project memory (`feedback_voice_target.md`) so it carries across sessions. The failure mode being targeted is marketing creep — "we believe", "honours the reader", "made in the right spirit", values-alignment-check lists — language that sells instead of describes. PRINCIPLES.md's earlier reference (added 2026-04-25) to architecture-review as the "tonal reference for all wiki and works content" was pointing at `_generated/PWA/architecture-review.html`, a path no fresh clone would have; this session resolved that by moving the file into `docs/architecture/`.

- **Did a `docs/` ↔ `_generated/` sync sweep** to follow up on the 2026-04-25 hoist. Promoted: `architecture-review.html` (canonical voice example) and `epub-a11y-w3c-review-2026-04-22.md` (technical reference shape) → `docs/architecture/`; `period-colours-research.md` + `period-colours-preview.html` → new `docs/design/` section. Demoted: Swedish `scalability-report-2026-04-15.md` (earlier snapshot of the English deep-dive that already covers the same ground) → `_generated/sessions/`; the three `ultraplan-brief-0{1,2,3}.md` (operational prompts for `/ultraplan`, not docs) → new `_generated/PWA/briefs/`. Deleted: stale duplicate of `lightrag-performance-report-2026-04-18.md` in `_generated/sessions/` (the `docs/` version is canonical, with both ingestion runs documented). Cross-references in `TODO.md`, `PRINCIPLES.md`, `pwa/README.md`, and `tl-pipeline-integration.md` updated. `docs/` now has four sections (architecture, editorial, design, pwa); `_generated/` is genuinely staging-only.

- **Codified the documentation-discipline convention** in a new `docs/editorial/CONVENTIONS.md`. Two patterns were already in informal use (`changelog:` frontmatter on evergreen docs like `tl-pipeline-integration.md` and `yjs-schema-and-sync.md`; top-of-doc Status banner on superseded plans like `_generated/sessions/tl-proofread-plan.md`); CONVENTIONS.md formalises which to use when. Evergreen docs (specs, principles, operational refs) get edited in place with `changelog:` frontmatter — body always reflects current truth. Dated reports (architecture-review, lightrag-performance-report, scalability-deep-dive, the `_generated/sessions/` files) keep their bodies untouched and grow a top-of-doc `<aside class="status-update">` block listing follow-up items with forward links. The body stays a record of what was known on its date; the Status block bridges to current truth. PRINCIPLES.md cross-references the new convention via a "Documentation discipline" bullet in the "How this shapes the work" section.

- **Applied the Status-block convention to `architecture-review.html`** as the first concrete example. Four follow-ups, each linking forward: Part 7 (cascade bug — fixed; see `tl-pipeline-integration.md` §4.6), Part 7/9 (determinism CI test — implemented; see §6.4), Part 9 (chapterUri validator — implemented; see §8.2), Part 6 (Yjs `TextualBody` silent-duplicate fix — resolved; see `yjs-schema-and-sync.md` §2.3). All four were resolved on 2026-04-24; the target docs' changelogs already linked *to* the architecture review. The Status block adds the missing reverse pointer so a reader landing on the featured doc sees in 30 seconds what's been resolved, without rewriting the body retroactively.

- **Taught `serve.py` to feature the architecture-review at the top of `INDEX.html` and to index orphan HTML.** Added a `FEATURED` tuple plus `.featured-section` / `.featured-card` CSS — the entry renders as a wide card above the section grids and is filtered out of its origin section to avoid duplication. Added `collect_orphan_html_files()` and polymorphic `extract_title`/`extract_lede` that dispatch on file extension, so hand-authored HTML (architecture-review.html, period-colours-preview.html) appears in INDEX automatically — title from `<title>` with the `— tolstoy.life docs` suffix stripped, lede from `<meta name="description">` falling through to the first `class="lede"` paragraph. Added `design` to `FOLDER_ORDER` and `FOLDER_META` (between editorial and pwa). Bumped the docs root font-size 137% → 150% on the rendered pages and matched architecture-review's 112.5% → 125% so they read at the same scale. Architecture-review also gained the standard site-header strip (breadcrumb back to `/INDEX.html`, matching the auto-injected one on `.md`-rendered docs) — added `--measure-wide` to its `:root` plus the `.site-header` / `.breadcrumb` CSS, ported from serve.py.

## 2026-04-27

- **Did a root ↔ website sync audit** to surface drift between the two repos' documentation. Report at `_generated/sync-audit/root-vs-website-2026-04-27.md`. Cross-checked against the architecture-review (which confirms `letters/` as a deliberate vault section, not aspirational — letters as ~10,000 indexed entries are why `works.json` needs to split per finding #16) and against LOG entries from 2026-04-25/26 (which established the `docs/` hoist, the open-project policy, and the Status-block convention). Conclusion: root and website are aligned on the broad strokes (mission, architecture, schema rules, contribution model). Drift is in the fine grain — schema versions not propagated to all references, target-state structure described as current state, `website/` lacking its own surface files.

- **Schema triplet cleanup.** Three unsynchronised schema files (`wiki-schema.md` v1.1, `tolstoy-person-schema.md` v1, `tolstoy-place-schema.md` v1) collapsed to one authoritative source. The v1 type-specific schemas had been silently superseded on 2026-04-16 when `wiki-schema.md` v1.1 added person and place templates; the v1 files were left in place but never updated, creating an apparent conflict (`nameEn`/`nameRu` in v1 vs `titleEn`/`titleRu` in v1.1). Verified the corpus uses `titleEn` (14 pages, 0 uses of `nameEn`) — the v1 names were dead documentation, not live code. Bumped `wiki-schema.md` to **v1.2** salvaging the richer optional fields from the v1 schemas before archiving them: `nameAlternatives`, `causeOfDeath`, `relationshipDescription`, `periodOfContact`, `authorityTier`, `occupation`, `religion`, `politicalViews`, `synopsis`, `worksAuthored/Translated/Edited/Transcribed`, `viaf`, `lccn`, `bnf` (person); `placeType`, `city`, `coordinates.approximate`, `roleInTolstoyLife`, `periodOfAssociation`, `synopsis`, `worksWrittenHere`, `geonames`, `openStreetMap` (place). All additions optional — no migration needed, existing wiki pages remain valid. Person and place templates updated with example values; "Field notes" subsections added explaining each new field. Originals copied to `website/schema/_archive/` with a README explaining the archival reason and pointing at v1.2.

- **Propagated v1.2 to the docs surface.** `MANIFEST.md` updated v5 → v6 for works (stale since 2026-04-16) and added the v1.2 wiki-schema reference, also expanded to mention all nine wiki types instead of the original four. `AGENTS.md` schema section reworded to name `wiki-schema.md` (v1.2) as the single source for all nine wiki types and to point at `_archive/` for the superseded files. `website/CLAUDE.md` rewritten — was a 2026-04-15 deprecation stub claiming consolidation into a parent `CLAUDE.md` that no longer exists in that form; now a short site-specific reference (schema versions, build commands, validators, Eleventy ignores) pointing at `../AGENTS.md` for the project documentation.

- **Flipped `tools` submodule URL from HTTPS to SSH** in `.gitmodules` for consistency with `website` (which became SSH on 2026-04-25 to avoid the credential-prompt issue). Both submodules now use `git@github.com:tolstoylife/...`. Pending: `git submodule sync && git -C tools fetch` to apply locally — bash sandbox couldn't run it reliably, recorded as a manual step.

- **Cleaned up Obsidian vault tracking in the website submodule.** `src/.obsidian/app.json` was deliberately committed at vault init (2026-03-31, `2d71a49`) to set vault conventions — new files in `wiki/`, attachments in `assets/images/wiki/`, wikilinks shortest-format; kept tracked. `workspace.json` was caught up in the same init by accident; gitignored on 2026-04-09 (`7b66b7f`) but never `git rm --cached`'d, so it kept showing as modified. Untracked it now. Added the small, stable rest of the vault config to git: `appearance.json`, `community-plugins.json`, `core-plugins.json`, `graph.json` — these document *how the vault works* (theme, plugin lists, graph settings) and are useful for collaborators or LLMs cloning the repo. `plugins/` and `themes/` stay ignored — vendored third-party code, size and licensing concerns. Replaced the over-broad `.obsidian` blanket rule in `.gitignore` with explicit ignores for the runtime-state and vendored files. Discovered a stray `.obsidian/` at the website repo root (4 nearly-empty files dated 2026-04-10, presumably created when Obsidian was once opened on the wrong folder); added `/.obsidian/` to the gitignore so it stays out without affecting `src/.obsidian/`. Also added `src/_design/` to gitignore — answers the audit's "commit, move, or reference" question for `design-system.md` with "keep as local-only working notes for now".

## 2026-04-29

- **Brainstormed and specced the timelinegraph feature** — a 2D knowledge-graph + timeline visualisation of Tolstoy's universe, intended for the landing page once the corpus passes ~200 nodes. Two-round research pass first (prior art + naming + library comparison; then 2D layouts that keep the "in space" feel + LOD), then structured brainstorm via `superpowers:brainstorming` (11 clarifying questions, 6 design sections each gated by approval), then implementation plan via `superpowers:writing-plans` (35 tasks, ~3,570 lines). Two design pivots worth recording: (1) rejected 3D entirely after the research surfaced the Wilke / data-to-viz / Bach STC consensus that time-as-filter beats time-as-axis, plus the live precedent of Republic of Letters; (2) switched mid-brainstorm from radial-around-Tolstoy (SDoFB Hooke) to a horizontal multi-lane spine + orbital cloud after Johan clarified that click should navigate to the wiki page, not recenter the graph — the SDoFB reference was about the legend (degree-of-separation rings, edge confidence), not the recenter mechanic. Final shape: D3 SVG spine over PIXI v8 WebGL cloud, four lanes (Works / Family Events / Historical Events / Letters), zoom = time resolution (decade → year → month → day) with click-and-drag pan in 2D, hover stub + click modal (no X button — click-out / Esc / swap), URL-controlled focus. The four-period colour system from `docs/design/period-colours-research.md` becomes the lifespan band's identity (Green/Red/Amber/Blue zones with vertical guidelines at 1851 / 1862 / 1880); Tolstoy's works inherit the period colour they were written in. Tolstoy himself is not rendered as a node — the colored band IS him; the family-event nodes deep-link to anchored sections of his wiki page. Implementation parked for a fresh session — 35 tasks × ~3 subagent dispatches each is a 15+ hour run, and Task 21 has a hard human-in-the-loop deuteranopia validation gate (Johan-eyes-on the red↔green boundary at 1851) before Track 3 can start. Spec: `docs/superpowers/specs/2026-04-29-timelinegraph-design.md` (`6ba0eec3`). Plan: `docs/superpowers/plans/2026-04-29-timelinegraph.md` (`7e987467`). Project home: `projects/timelinegraph/` (gitignored, local-only). Resume by invoking `superpowers:subagent-driven-development` against the plan.

## 2026-05-04

- **Continued the timelinegraph prototype track** from `prototypes/07-parallax-camera/` (built earlier today; per-iteration `NOTES.md` recorded but no LOG entry written for that earlier session) through `08-zoom-reveal/` and into `09-graph-time/`. Three iterations all dated 2026-05-04, each building on the last. The project home `projects/timelinegraph/` is gitignored, so this LOG entry is the only tracked record of the arc — fine-grained handoffs live in each prototype's `NOTES.md`.

- **08-zoom-reveal** introduced a two-tier visual on top of 07's dolly camera: a per-work `minors` array (~7 fabricated characters and motifs per work — Anna Karenina gets *Anna*, *Vronsky*, *Levin*, *Kitty*, *Karenin*, *Stiva*, *Dolly*, *the train station*, *the mowing scene*, *'Vengeance is mine'*; ~83 minor nodes total across the 12 works), force-laid-out around each work's bar X. At default zoom they render as faint grey distance dots (opacity 0.13, no labels) — the "graph in the fog" feel. Click a work and a 750 ms cubic-eased `animateView` zooms the viewport to centre that work, brightening its minors to full color and drawing dashed connecting edges from the bar to each. Persistent card from 07 dropped in favour of tooltips only. Lanes narrowed (`LANE_H_W` 50 → 32, `SPINE_H_W` 200 → 128) and period-band-inside-spine opacity dropped 0.14 → 0.075. Late in the iteration, on Johan's prompt: Esc / click-outside no longer resets to default view — just clears the spotlight; current zoom stays.

- **09-graph-time** pivoted away from the foreground/background tier visual entirely. Johan reframed the goal mid-session: the timelinegraph is "the real wiki graph projected onto a temporal axis," not "a timeline with hover info." Works are major nodes that happen to be displayed as line segments because they have duration; everything else is a graph node that should look the same, sized by wikilink degree, with always-on edges — the Obsidian graph aesthetic, with time as a backbone. So 09 collapses `renderCloud` and `renderMinor` into a single `renderNodes` (uniform circles, type-coloured, sized by `r = NODE_R_BASE + 1.4 × deg^0.55` clamped 4–13 px), adds 130 fabricated cross-edges in `extraEdges` so degree is meaningful (Sofia↔Natasha, Yasnaya↔Levin, Caucasus↔Hadji-Murad, Pierre↔Andrei, Anna↔Vronsky, etc., totalling ~252 edges across work-major + work-minor + extras), draws an always-on edge web (default opacity 0.075; selection brightens incident edges to 0.7 in the *other* endpoint's colour, fades non-incident to 0.025), and replaces 08's two force passes with a single live d3-force simulation (link force + x-anchor + y-zone + collide; cloud is `fx`-pinned to its year, minors are `forceX`-anchored to their work's bar centre — so cross-period edges actually tug connected entities toward each other). Tooltip carries `deg N`. Sidebar tier toggles (cloud / minor) collapsed into single Edges + Labels toggles.

- **Spec divergence is now substantial.** The approved spec (`docs/superpowers/specs/2026-04-29-timelinegraph-design.md`) said zoom = X-only and didn't anticipate degree-based sizing or always-on edges. 07 broke the X-only rule with anisotropic-0.5 Y-zoom; 08 carried it forward; 09 added two more departures (degree sizing, always-on edge web) plus the tier-collapse. The spec is overdue for amendment, but the visual baseline isn't settled yet — 09's default state has too many overlapping signals (period bands, lane separators, work bars, edge web, varying-size nodes, lots of cross-period edges crossing the spine), which Johan flagged at session end: *"the complexity with this solution is a problem I think we can solve in prototype 10."*

- **P10 is the complexity-reduction pass** (next session). Candidate directions parked in `projects/timelinegraph/TODO.md` and `prototypes/09-graph-time/NOTES.md`: edges to `<canvas>` (halves render cost, unlocks cheap edge bundling and per-edge alpha modulation); edges-on-demand (hide most at default zoom, reveal on hover/select — biggest single win, costs the Obsidian-graph-at-rest feel); top-N edge filter; edge bundling / curved routing; period-band fade-on-zoom; gating low-degree minors behind a zoom threshold; reconsidering whether visible lane separators add value at low zoom. Pick a small set at the start of P10, not all. Real-wiki-data integration (replacing hardcoded `cloudData` / `worksData` / `extraEdges` with parsed `[[wikilinks]]` from `website/src/wiki/`, `works/`, `letters/`, `sources/`) stays gated on visual settling — don't start it before P10 lands. Once 10 settles, amend the spec and start Track 0 of `docs/superpowers/plans/2026-04-29-timelinegraph.md`.

- **Project memory updated.** `project_timelinegraph_07_direction.md` rewritten to cover the 07–09 arc and the complexity problem; `MEMORY.md` index line repointed at `prototypes/09-graph-time/NOTES.md` as the resume entry. P08 and P09 each got their own `NOTES.md` handoff. P07's earlier session never wrote a LOG entry; that gap is now closed by this multi-paragraph entry covering all three iterations.

- **Resumed timelinegraph in a second session later the same day, and built prototype 10.** Johan reframed the complexity problem at the top of the session: instead of tuning the unified-projection model (canvas-edges, top-N filter, edges-on-demand — the candidates parked on P09's NOTES), separate the two contradicting models entirely. The proposed shape was a four-column split: sidebar with the existing entity-type filters, a pure Obsidian-style force-directed graph at ~60% width, a narrow vertical timeline strip, and a stub-text panel on the right. Click a node and the timeline zooms+pans to that entity's date region; the panel fills with the stub. Walked the design through `superpowers:brainstorming` (10 clarifying questions, 5 design sections each gated by approval), wrote the spec to `docs/superpowers/specs/2026-05-04-timelinegraph-prototype-10-design.md` (skipping the formal `writing-plans` step on Johan's call — "for a single-file HTML prototype the spec already is the plan"), then built the prototype directly.

- **10-split-view** confirms the structural shift solves the complexity problem. The graph is pure d3-force (charge + link + collide + center, `alphaDecay: 0.012`, no `forceX/forceY`); nodes are draggable with d3-drag and stay pinned where you drop them (`fx/fy`); double-click releases the pin. Scrollwheel zoom on the graph (range 0.4×–4× via d3-zoom on a `gView` transform group; wheel-only filter so background drag still clears the selection). Vertical timeline strip with period-coloured bands (Youth → Soldier → Great Works → Prophet, opacity 0.14 in-life / 0.04 pre-post after eyes-on tuning); year ticks on the left edge with adaptive granularity (10y → 5y → 1y → quarters → months); event-mark dots on the right edge; clickable so they select the matching event-node in the graph (option A from the brainstorm — bidirectional, but limited to event-marks). Selection-driven timeline zoom: 750 ms `easeInOutCubic` (respects `prefers-reduced-motion`); spans pulled from `(written, published)` for works, `year ± 2.5` for events, `dateRange` if defined for cloud entities (added to Sofia, Yasnaya, Caucasus, Sevastopol, Chertkov, Non-resistance, Tolstoyanism, Gandhi, Masha, Moscow), else `year ± 8` fallback. Right panel renders welcome state at rest (title + hint + colour legend + about-paragraph) and a selection state on click (title in entity colour, type badge, period chip for works, date line, full `desc`, "Connected" list grouped by type with clickable buttons that re-run the selection cycle, disabled "View full article →" affordance with tooltip explaining real wiki articles arrive later). Minor nodes are hidden by default and reveal as a **separate satellite layer** around a clicked work — radial arrangement at ~80px, fade-in 250 ms, moves with the work as it drifts; the global force layout is never disturbed by satellite reveal.

- **Mid-session refinements (Johan-eyes-on, in order):** right panel widened 380 → 418 px (+10%); scrollwheel zoom added to the graph with reset-button restoration to identity; timeline period-band opacity dropped 0.30 → 0.14 (in-life) and 0.10 → 0.04 (pre/post). Final read: *"This is going to be awesome."*

- **Spec amendment status.** The canonical spec (`docs/superpowers/specs/2026-04-29-timelinegraph-design.md`) is now even more out of date than after P09 — it described a unified-projection v1, and P10 makes the graph and timeline physically separate components. The 2026-05-04 prototype-10 design doc complements the canonical rather than replacing it; the rewrite of the canonical can wait until real-wiki integration in P11+ surfaces any further structural changes. Production-track plan (`docs/superpowers/plans/2026-04-29-timelinegraph.md`, Tracks 0–4) still hasn't started; P11 candidates listed in `projects/timelinegraph/TODO.md` (real-wiki integration is the highest-value next move now that structure is settled).

- **Project memory updated again.** `project_timelinegraph_07_direction.md` rewritten to cover the full 07–10 arc and the structural pivot; `MEMORY.md` index line repointed at `prototypes/10-split-view/NOTES.md` as the resume entry. P10 has its own `NOTES.md` handoff matching the 08/09 pattern.

## 2026-05-09

- **Completed Task 1b of the splash-site plan — applied the Tolstoy period palette + warm parchment / slate charcoal surfaces to the website design system.** Built on top of Task 1a's port (commits `03c6eb9` → `34e88d7` on `port-jedee-design-system`); shipped as a single commit `9a9fe0d` after one extra consolidation step that wasn't in the original handoff. The branch is now 8 commits ahead of `main` and not merged — Johan's call. Source authority chain is `docs/design/period-colours-research.md` (palette rationale, 2026-04-19) → `docs/design/period-colours-preview.html` (refined hex values + dark-theme tints, more recent) → committed values in [`website/src/_data/designTokens/colorsBase.json`](website/src/_data/designTokens/colorsBase.json). Where preview disagreed with research-doc proposals, preview won — research-doc explicitly framed its values as "starting points for refinement." Two such divergences landed: Period III amber ochre `#C8860A` → preview's gold-leaning `#B8960A`, Period IV Prussian `#2B5F8E` → preview's brighter `#2362A0`. Periods I (forest green `#4A7C59`) and II (deep crimson `#B83232`) committed unchanged from research.

- **Surface design landed on symmetric base-* ramp.** The original JEDEE arrangement was asymmetric — light theme `bg` sourced from `palette[].base-lightest`, dark theme `bg` sourced from `shades_neutral[].Gray`'s 900 stop, even though both anchors happened to be `#495464`. Task 1b put four meaningful values into `palette[]` (`base-darkest #272b30`, `base-dark #30353c`, `base-light #dcd9d0`, `base-lightest #e8e4db`) and rewired `semanticColors.json` so both themes' surfaces source from `base-*` (light from `base.light/lightest`, dark from `base.darkest/dark`). `shades_neutral.Gray` was repurposed to `#9ba3b4` — the preview file's `--theme-mid` — as a cool-grey complement that drives text-accent, dividers, and tertiary surfaces. Cleanly separates the warm parchment / slate charcoal axis from the neutral mid-tone scaffold.

- **Per-period dark-theme tints added as standalone tokens.** The `light_dark` schema only generates a `subdued` sibling per accent (–20% chroma at the same lightness) — not enough lift for legibility on `#272b30`. Added four `standalone[]` entries (`amber.tint #DDB840`, `red.tint #E06868`, `blue.tint #5DA0DC`, `green.tint #7DBF92`) so they generate as proper Penpot tokens rather than living only in CSS. `variables.css`'s dark-theme blocks now resolve `--color-accent-{amber,red,blue,green}` to the matching `*-tint`. Light theme keeps the full-chroma values. The accent-tinting strategy means components reference `--color-accent-amber` and the theme block handles the swap — no per-component dark/light branching.

- **Rename Orange → Amber + sweep.** The slot was named `Orange` from JEDEE and shipped through Task 1a as JEDEE-default `#EF7E40`. Renaming it now (Approach A from the handoff) avoided the technical debt of "Amber values in an Orange-named slot." Sweep covered 16 consumer CSS files plus `index.njk` and `styleguide.njk`: `--color-orange-*` → `--color-amber-*`, `--color-accent-orange` → `--color-accent-amber`. Added `--color-accent-red` explicitly — JEDEE only carried orange + blue + green (three accents), Tolstoy needs four (one per period). The styleguide `customColors` array picked up the red entry. Code-highlighting `--color-code-orange` and the `debug.css` literal-orange warning outline left alone (different namespace, not part of the design accent system).

- **Build-script rebrand.** `src/_config/setup/build-design-md.js` and `build-penpot-tokens.js` carried JEDEE branding through Task 1a deliberately — Task 1b unfroze: `META.name` JEDEE → tolstoy.life, description rewritten to name the four periods explicitly, `SEMANTIC_ROLES.secondary` orange-500 → amber-500, COMPONENTS map's three accent-orange references swept to accent-amber. The accent-tints loop replaced the old subdued-only one. Console message in `build-penpot-tokens.js` retoned. Pipeline regenerated cleanly: 92 tokens (was 91 in 1a), 21 references resolved, no broken refs.

- **Consolidated `src/_design/design-system.md` into `DESIGN.md` body — was outside the original handoff but Johan flagged the duplication mid-session.** The two files were doing the same job split: `DESIGN.md` (root, build-script-managed) had auto-generated frontmatter plus a JEDEE placeholder body that nobody ever wrote; `src/_design/design-system.md` (untracked, never rendered, no frontmatter) had 16 KB of full hand-written editorial prose. The build script's docstring already states the intent — "humans own the prose; this script owns the tokens" — so the right home for the editorial prose is `DESIGN.md`'s body, which `build-design-md.js` reads + preserves verbatim across regenerations. Migrated the prose into the alpha-spec section structure (Overview / Colors / Typography / Layout / Elevation & Depth / Shapes / Components / Do's and Don'ts), trimmed redundant token tables that the frontmatter already covers, deleted `src/_design/`. Verified body preservation across one round-trip of `npm run penpot:tokens` — body untouched, frontmatter regenerated. `_design/` was untracked so its deletion is invisible to git status.

- **Open Question 4 from `period-colours-research.md` deferred to the timelinegraph track.** That question — "is the knowledge graph interactive (D3.js / WebGL) or a static illustration" — is out of scope for design-system tokens. The 07–10 prototype arc plus `docs/superpowers/specs/2026-04-29-timelinegraph-design.md` is the live answer. The other three open questions (Period II naming, gold vs amber for Period III, forest vs spring green for Period I) are resolved in the new "Decisions landed — 2026-05-09" section appended to `period-colours-research.md`.

- **Visual verification with Claude Preview** confirmed light theme renders correctly: `--color-bg = #dcd9d0`, `--color-text = #272b30`, all four `--color-accent-*` values resolve to the period-palette hexes (computed style readback), homepage masonry renders the four period colours where it was previously rendering JEDEE orange/Prussian/olive. Dark-mode visual confirmation deferred — `prefers-color-scheme: dark` emulation needs a reload after `preview_resize`, and the screenshot didn't update before Johan called the verification done. Confirmed this is fine because Johan will tweak values in Penpot once the import lands; the structural correctness (token resolution, surface mapping, accent tinting) was the gate, and that's verified.

- **Pending follow-ups.** Penpot import is Johan's manual step (open the tolstoy.life Penpot file, Tokens panel → import → select `website/tokens/penpot-tokens.dtcg.json`; if Light/Dark themes import empty, set membership manually per `docs/design/penpot-tokens.md`). Dark-mode contrast audit for the four period accents on `#272b30` (WCAG AA — 3:1 for UI, 4.5:1 for text) — recorded in `DESIGN.md`'s "Open" list and `period-colours-research.md`'s decisions section, not in TODO.md (splash-site work continues to be tracked in `docs/splash-site-plan.md`, not the platform priority backlog). Task 1c (fonts) is the natural next session on this branch; Task 2 (license + SDG) and Task 3 (splash homepage rebuild) are downstream of that.

## 2026-05-11

- **Took the prose half of Task 2 from the splash-site plan — LICENSE tightening + source-mode design.** Started from the compatibility question of running the project's SDG public-domain dedication alongside the CC BY-SA licence on `primary-sources/tolstoydigital-TEI/`. Verified the upstream licence by reading the `<availability>` block in `reference/personList.xml` (and a sample work XML) directly — every file declares "Creative Commons Attribution Share-Alike (cc by-sa)" without a version number. This project now treats the upstream licence as CC BY-SA 4.0 unless tolstoydigital pins it otherwise; flagged as a follow-up to ask the tolstoydigital team to specify a version in their `<availability>` block.

- **LICENSE rewritten with two structural additions.** Inserted a new **"Licence boundaries inside this repository"** section between the source-texts paragraph and the third-party-dependencies section, declaring the directory-level split: SDG (functionally equivalent to CC0-1.0) for `website/src/wiki/`, `website/src/sources/`, `website/src/letters/`, `docs/`, and all original code/prose; CC BY-SA 4.0 (SPDX `CC-BY-SA-4.0`) for `primary-sources/tolstoydigital-TEI/` and any `_generated/` file declaring that licence in its header; public domain for Tolstoy's own writings regardless of transcription source. The third-party tolstoydigital paragraph was rewritten to pin the version assumption and to point at the new source-mode editorial spec for per-fact attribution. SPDX identifiers added throughout so the licence boundaries are machine-readable.

- **Source mode — the editorial mechanism for per-fact attribution.** Spec at `docs/editorial/source-mode.md`. Standard markdown footnotes (`[^1]` … `[^n]`), no custom prefix, no shortcode — `markdown-it-footnote` is already in Lene's plugin chain at `website/src/_config/plugins/markdown.js`. The single innovation over Wikipedia's pattern is a licence-tag rule: every footnote ends with `(PD)` / `(CC BY-SA 4.0)` / `(CC BY 4.0)` / `(fair use)` / `(restricted)`. The tag does double duty as legal attribution (BY) and as the editorial proof of "verifiable facts only" (the wiki editorial principle). Toggle is binary — off (default) shows a "Sources used on this page" summary; on shows Wikipedia-style `[1]` markers + a References block. Default off is deliberate: the site promotes reading; verification is on demand. The pre-existing `fieldSources` frontmatter in `website/schema/wiki-schema.md` stays — it serves structured per-field citation, complementary to the prose footnotes.

- **Mid-session course correction on complexity.** First draft of source mode included a three-state toggle (off / markers / verbose), a custom `[^src-...]` footnote-ID prefix, a lint script, and a `recordStatus: verified` quality tier — Johan pushed back, citing Wikipedia's far simpler References pattern. The simpler version (binary toggle, plain numbered footnotes, licence-tag in footnote text) was adopted and now is the spec. Saved as a feedback memory: prefer Wikipedia-class familiar patterns over custom systems; only add machinery when the simple version provably fails.

- **Scaffolded `docs/development/` as a peer of architecture/design/editorial.** Two new files: `docs/development/README.md` (landing: what lives here vs neighbouring folders, references to upstream Eleventy Excellent / CUBE CSS / Every Layout / Lean Web) and `docs/development/source-mode-implementation.md` (a feature recipe — confirms what already exists in the codebase, names the four pieces to add later in stages: CSS block at `website/src/assets/css/global/blocks/source-mode.css`, `<template>`-injected toggle button + vanilla ESM component mirroring the theme-toggle pattern, a build-time "Sources used on this page" summary as an Eleventy filter, an optional later licence-tag lint). `frontend.md` and `backend.md` overview docs deliberately deferred until features have shipped — writing them now would codify guesses rather than patterns. No code shipped this session; the recipe is the deliverable.

- **AGENTS.md folder-list updated** to add `development/` and `research/` to the tracked `docs/` subfolders inventory (line 186). `docs/INDEX.html` is auto-generated by `docs/serve.py` and will pick up the new files on next regeneration — not hand-edited.

- **Status of Task 2 from the splash-site plan.** The LICENSE prose-tightening half is complete. The SDG graphic integration (illustrator's vectorized lettering, with and without caption) is still pending illustrator delivery — recorded inline at `docs/design/splash-site-plan.md` Task 2.

- **Migrated `docs/` from a documentation hub into a dated dev-blog — phases 1, 2, and 3.1 of four.** Plan + decisions drafted as the inaugural entry at `docs/design/2026-05-11-docs-to-blog-migration.md`. Decisions: (1) the dev blog becomes the `notes` collection on the eleventy site, mirroring `~/Projects/JEDEE/src/posts/notes/`'s standard eleventy-excellent setup — single-word name matching `LOG/MANIFEST/TODO/ROADMAP` convention, reads as a research notebook rather than a corporate dev blog; (2) `docs/` and `serve.py` are temporary scaffolding — both retire once the eleventy collection holds everything; (3) backdating uses the git first-commit date in this repo, except where the filename already encodes a more semantically meaningful date (e.g. `lightrag-performance-report-2026-04-18.md` keeps 2026-04-18, not its 2026-04-25 commit date); (4) no RSS generation in `serve.py` — eleventy-excellent handles content-collection RSS at `website/`, so RSS lands free in Phase 3.

- **Phase 1 (commit `f9d53d31`): annotated every `docs/*.md` with `layer:` / `date:` (or `lastUpdated:`) / `tags:` frontmatter.** 28 files, idempotent Python script, preserved existing frontmatter where present (e.g. `editorial/source-mode.md` kept its `title:` / `lastUpdated:` / `changelog:` and gained only `layer: reference` + `tags: [editorial]`). 14 files reference, 14 blog. One per-file decision needed via `AskUserQuestion`: the three splash-site-plan files chronologically go `-original` (v1) → `-revised` (v2) → `splash-site-plan.md` (v3, current), even though git first-commit dates are inverted (`-original` was preserved deliberately *after* the rewrites). One unexpected state: `docs/design/penpot-tokens.md` had `r--------` permissions; git log showed nothing intentional, chmod 644 and continued.

- **Phase 2 (commit `351bdb5c`): rewrote `serve.py`'s INDEX as a chronological blog feed + reference appendix.** Drops the `FEATURED` constant (added 2026-04-26) — in a chronological feed the top entry *is* the featured entry. Adds `extract_meta()` that reads `layer:` / `date:` / `lastUpdated:` from frontmatter and falls back to mtime for missing dates; files without an explicit layer default to `reference`. Hand-authored HTML orphans (`architecture-review.html`, `period-colours-preview.html`) carry no YAML frontmatter, so they're categorized via a small `HTML_META` dict at the top of `serve.py`. The new INDEX: blog entries sorted by date (newest first) grouped by year, reference shown below in the existing index-grid cards, labelled "Reference — pending port to website/src/notes/". The `.featured-*` CSS remains unused but harmless; retires alongside `serve.py` itself.

- **Phase 3 step 1 (website `79d2540`, parent pointer bump `ab8833a4`): scaffolded the eleventy `notes` collection.** The standard eleventy-excellent notes pattern was already half-wired in this site (`_layouts/note.njk`, `pages/notes.njk`, `partials/card-notes.njk`, `getAllNotes` in `_config/collections.js`), but the content folder at `src/posts/notes/` had never been created. Three additions: `src/posts/notes/notes.json` (collection config mirrored from JEDEE — Johan's reference implementation), `src/posts/notes/Starting a development blog.md` as the inaugural entry (eleventy pagination doesn't render the list page if the collection is empty, so a real first post was required — not a placeholder, real intro copy explaining what Notes is for), and `src/_data/navigation.js` to add Notes between Wiki and About. Build runs clean (51 files, 1.93s, no errors); `feed.xml` automatically picks up the note via the `tags: "posts"` directive in `notes.json`; `/notes/starting-a-development-blog/` resolves. Two open questions defer to Phase 3 step 2: (a) whether `description:` frontmatter is auto-derived from each file's first paragraph or hand-written per file; (b) where the 14 *reference* files go (Notes is for dated entries, not evergreen reference — candidates: `wiki/`, a new top-level collection like `reference/` or `handbook/`, or defer the reference port indefinitely).

- **Incidental cleanup bundled into Phase 1.** All the splash-site-plan changes that were uncommitted at session start (frontmatter additions to all three plans, H1 version suffixes, `serve.py` `FOLDER_META` extension with `research`+`development`) turned out to be Johan's in-flight prep for exactly this migration — bundled into the Phase-1 commit rather than committed separately, since they were the same conceptual change.

- **Phase 3 step 2 (website `3c748db`, parent `88e4d8dd`): ported all 14 `layer:blog` files from `docs/` into `website/src/posts/notes/`.** Filename convention: `YYYY-MM-DD-slug.md`. Frontmatter adapted per file: dropped `layer:`, `status:`, `owner:`, `project:`, `supersedes:`, `parent-spec:`, `lastUpdated:`, `changelog:`; added hand-written `description:` and `draft: false`; normalised `tags:` to the allowed set (`architecture`, `design`, `development`, `pwa`, `superpowers`). One file required `templateEngineOverride: md` — `source-mode-implementation.md` contains a `{% css "local" %}` reference in a table cell that Nunjucks would otherwise try to evaluate. Source files deleted from `docs/`; canonical URLs are now on the eleventy site at `/notes/{slug}/`. Build clean: 74 files written (up from 51 before the port). Open questions resolved this session: description hand-written per file (auto-derived first paragraphs were too rough for the card layout); permalink uses default `/notes/{slug}/` not date-prefixed (consistent with inaugural entry, date visible on cards via `date.njk`). `docs/` now contains only the 14 `layer:reference` files — `serve.py` reference appendix continues to work correctly.

- **Phase 3 step 3 — defer all 14 reference files.** Read all 14 files end-to-end. They split into two audiences, not one: ten are engineering/operational reference (the five PWA specs, `architecture/internal-operations.md`, `design/penpot-tokens.md`, `development/README.md`, `editorial/conventions.md`, `research/pss-volume-mapping.md`) that serve contributors, not site readers — moving them to the eleventy site would only make cross-referencing from the codebase harder; four are project transparency (`editorial/editorial.md`, `editorial/source-mode.md`, `research/tolstoydigital-tei-reference.md`, `research/copyright-renunciation/index.md`) that *could* live on the site as "about the project" pages, but the section to land them in doesn't exist yet. Decision: keep all 14 in `docs/` indefinitely. Group B revisits when an `about/` or `project/` section gets built. `serve.py` reference-appendix label retoned to match: "pending port to website/src/notes/" → "engineering specs and operational notes". Intro paragraph rewritten to point dated entries at `tolstoy.life/notes/` and frame the bottom section as engineering reference that lives here by design, not by deferral.

- **Phase 4 — dropped 28 generated `.html` siblings from git.** Two cohorts: 14 are regenerated by `serve.py` on demand from their `.md` siblings; 14 were stale renderings whose `.md` was ported to `website/src/posts/notes/` in step 2 and never cleaned up. `docs/.gitignore` (new file) ignores `*.html` recursively with three explicit exceptions — `/INDEX.html` (entry point), `/architecture/architecture-review.html` and `/design/period-colours-preview.html` (hand-authored orphans, declared in `HTML_META`). The leading-slash anchors are load-bearing: a bare `!INDEX.html` would re-include `research/copyright-renunciation/index.html` on case-insensitive macOS filesystems — caught by `git status` showing the file as untracked rather than ignored. After Phase 4: `serve.py --build-only` reports "14 documents converted + 2 hand-authored HTML" (down from 16 hand-authored HTML before the cleanup), and `git status docs/` is clean except for the staged deletions and the new `.gitignore`. The Phase 4 entry in the migration plan said "Keep `INDEX.html` tracked because it's the published entry point"; that condition is now self-documented by the gitignore exception itself.

## 2026-05-12

- **Notes URLs, duplicate `<h1>`, stray "posts" tag chip — Phase 3 follow-up (website `c98e7f0`).** Three small defects surfaced once the ported notes were live. (1) `notes.json` derived the permalink from `{{ title | slugify }}`, so a title containing literal `/` or `→` (e.g. `"docs/ → dev blog migration plan"`) leaked into the URL as `/notes/docs/-%E2%86%92-dev-blog-migration-plan/` — slugify keeps `/` and URL-encodes `→`. Switched to `{{ page.fileSlug | slugify }}`; Eleventy strips the `YYYY-MM-DD-` prefix from `fileSlug` automatically, so URLs are clean and date-less (`/notes/docs-to-blog-migration/`) and don't change if a title is edited. Renamed `Starting a development blog.md` → `2026-05-11-starting-a-development-blog.md` (`git mv`, history preserved) to fit the new convention. (2) `_layouts/note.njk:8` already renders `<h1>{{ title }}</h1>`, and 14 of 15 note files also opened with `# Heading` as the first body line — doubled `<h1>`s on every page. Stripped the leading heading + blank line from each. (3) The per-note tag-chip loop at `note.njk:27` filtered out `"notes"` but not `"posts"`, so every note rendered a "Posts" chip that linked to `/tags/posts/` — a 404, because `tagList` in `_config/collections.js` already excludes `posts` from the tag-index. Widened the filter to `tag not in ["notes", "posts"]`. Verified against the built `dist/`: 15 clean URLs, exactly one `<h1>` per page, no `posts`/`notes` chips.

- **Removed the eleventastic service worker (website `7ecdf4f`) — Stage-1 determinism gate, one of two known drifts resolved.** GitHub's `Build determinism` workflow had been red since 2026-05-10. Logs showed a single non-deterministic file in the latest run (`serviceworker.js`); the previous failing run had it plus `feed.xml`. Cause was as documented in 2026-04-24's check-determinism analysis: `src/pages/serviceworker.njk` embedded `'cache-{{ buildTime|toIsoString }}'` into `CACHE_NAME`, and `buildTime` was a global `new Date()` defined in `eleventy.config.js:33`. Two builds 6 seconds apart got two different ISO timestamps → two different SHA-256s. The original plan was to wait for Workbox to replace this in Stage 1; Johan opted to remove now and rely on the future Workbox install for the real PWA capability. Deleted four touch-points: `src/pages/serviceworker.njk` (the source), `src/_includes/head/js-serviceworker.njk` (orphan registration include — not actually `include`d anywhere; dead since some prior refactor), the inline `navigator.serviceWorker.register('/serviceworker.js')` block in `_layouts/base.njk:58-62` (would otherwise 404 on every page load), and the `buildTime` global in `eleventy.config.js:31-33` (grep confirmed nothing else used it). Ran `.github/scripts/check-determinism.mjs` locally: `✓ Deterministic — 294 files, identical across both builds`. The CI workflow becomes a candidate for "required status check" once the `feed.xml` drift also resolves (still shelved per 2026-04-24: needs the first post or a "no blog" decision to give the Atom `<updated>` fallback a stable date). TODO priority 5 A3 updated accordingly.

- **Submodule push deferred to Johan.** Two website commits (`c98e7f0`, `7ecdf4f`) are local; parent records the older `3c748db` and will be bumped to `7ecdf4f` once they're on `origin/main` (otherwise a fresh `git clone --recurse-submodules` of the parent fails: it tries to fetch a SHA GitHub doesn't have).

- **Timelinegraph — prototype 11 (`projects/timelinegraph` `fe93687`, repo initialised).** Pivoted the timeline from P10's 90px vertical strip on the right to a 180px **horizontal bottom pane** split into three calm bands — Works (multi-lane Gantt) · Family · History. Added the six interaction asks Johan listed: (1) drag the graph background to pan (d3-zoom filter routes mousedown only when the target isn't a `.node` / `.sat-node`, so node-drag still wins); (2) clicking on empty graph background resets the timeline view to 1828–1910, with a mousedown/click 4-px movement threshold to distinguish drag from click; (3) new sidebar "Jump to" lists people (alpha) and events (chrono) — clicking pans the graph viewport to that node + adds a subtle `wobble()` (small `sim.alpha` bump); reset gets a smaller wobble (*"a little of the same movement"*); (4) timeline zooms past P10's quarter-year granularity into months → weeks → days, with crowding-aware tick labels; (5) all event marks render as rounded-end capsules covering their date range, with a dot fallback when `e.date` is an ISO day (added day precision to marriage 1862-09-23, departure 1910-10-28, emancipation 1861-03-03, etc.) or when the capsule width < 3 px; wars and the famine become natural capsule-lines (Crimean 1853–1856, Franco-Prussian 1870–1871, Russo-Turkish 1877–1878, Famine 1891–1892, 1905 Revolution 1905–1907); (6) clicking a character inside a work's satellite layer (e.g. Vronsky inside Anna Karenina) now sub-selects rather than replacing — the work stays selected, sibling minors dim to 0.25 opacity, the right panel switches to the minor's stub with a breadcrumb chip back to the work. Family state-lines kept curated to two (Married 1862–1910, Crisis 1879–1882) rendered as faint 3 px capsules at 0.30 opacity — a per-child-or-friendship state-line would crowd the band fast; better to add lines only where duration is the point. Johan-confirmed: *"supernice, there might be some tweaking needed but really good."* Tweak passes flagged for next session: band height ratios (Works carries 3 lanes), work-capsule title overflow at zoomed-out density, day-level tick density at extreme zoom, balance of the marriage state-line vs the moment-dots. Discovered along the way that `projects/timelinegraph/` had never been a git repo despite the documented "each production project carries its own .git" convention; `projects/splash-tolstoy-quote/` is in the same state. Initialised the timelinegraph repo with a minimal `.gitignore` (excludes `.omc/`, `.claude/settings.local.json`, `.claude/worktrees/`, `.DS_Store`); first commit captures prototypes 01–11 + MANIFEST/RESEARCH/TODO. No remote yet — `git remote add origin …` whenever it's time to publish. Memory `project_timelinegraph_07_direction.md` extended to cover P11 and the repo state.

- **Project sweep — stray files, inconsistencies, and stale refs.** Three parallel Explore agents audited structure, doc consistency, and git hygiene; findings cross-checked against the filesystem and git history. Changes applied: (1) **AGENTS.md** corrected in three places — `tools/` line in the project tree (`(submodule)` → `(separately cloned, not a submodule)`), PWA pointer at line 219 (removed the non-existent `stage-1-implementation.md` from the file list; added a redirect note to `/notes/stage-1-implementation/`), and the "Tools (ebook toolset)" section (added explicit "not a submodule" note with the clone URL and the 2026-05-10 removal commit). The `tools/` confusion was introduced by commit `47a5eea0` (2026-05-10) which removed `tools` from `.gitmodules` but didn't update AGENTS.md. (2) **Four `docs/` files** updated to reflect that `stage-1-implementation.md` was ported to the notes collection on 2026-05-11 (not absorbed, as a casual LOG reading implied): `docs/pwa/README.md` (section #3 now flags the move, /ultraplan example prompt updated, "five documents" → "four"), `docs/pwa/tl-pipeline-integration.md` (sibling-doc reference and §9 cross-ref updated), `docs/pwa/wiki-integration.md` (removed from sibling list), `docs/editorial/conventions.md` (removed from evergreen-docs example list). (3) **Git hygiene**: pruned 3 stale worktrees in `_generated/PWA/.claude/worktrees/` (prunable — gitdir pointed to non-existent locations); deleted 27 `claude/*` session branches (1 `[gone]` from origin + 26 whose commits were already on `main` — all from `team`/autopilot fan-out on 2026-04-29 and 2026-05-04 timelinegraph sessions). Branch count: 31 → 4 (`main` + 3 active worktree branches). Worktree entries: 7 → 4. (4) Cleared all `.DS_Store` files outside `__backup/` and `__scrap/`.

## 2026-05-13

- **Planning session — proposed a capture convention for byproduct data from primary-source task sessions.** Triggered by the 2026-05-10 splash-tolstoy-quote session, whose Johan-facing scratchpad lived at `projects/splash-tolstoy-quote/` and was almost lost: `projects/` is gitignored at the parent and the subproject was never `git init`'d (same state `projects/timelinegraph/` was in until 2026-05-12). Output: `_generated/research/research-practices.md` (eight sections — framing, inventory, current-state audit, options comparison, recommendation, no-migration, four open questions, scope). Local-only by design per Johan's mid-session call. Recommendation in brief: two-zone file convention — `_generated/research/<topic>/` for working byproducts (gitignored), `docs/research/<topic>/` for polished output that gets promoted when ready. Inside the working zone, a small prescribed file set (`index.md` draft, `hits.md`, `method.md`, `negative-results.md`, `open-questions.md`, `recommendations.md`, plus `extracts/` and `scripts/`) created as content appears, not as empty stubs. The minimum-viable rule that prevents the 2026-05-10 near-loss: scratchpad in `_generated/research/<topic>/` from day one, never in `projects/<topic>/`; `projects/` stays reserved for subprojects with their own git repo. Framing rule: call this "study notes" / "session byproducts", not "research" — the LLM Wiki is where research findings land. Vocabulary chosen carefully because Johan flagged mid-brainstorm that the triggering session was an editorial task (find a splash quote), not scholarly research.

- **Proposal accepted — all four open questions resolved on Johan's recommended defaults.** (1) Only-as-content-appears, not empty stubs at session start — the prescribed file set is a vocabulary, not a checklist. (2) LightRAG indexing of `_generated/research/` flagged as a Phase-3 follow-on; today's `lightrag/` indexes the vault only, the working zone joins later once enough material accumulates that semantic retrieval beats re-sweeping. (3) `docs/research/` name kept as-is and tagged for a future sweep — slightly grand for what it holds but renaming now is premature. (4) The 2026-05-10 lost byproducts are not retroactively recaptured; the keyword set in the polished essay's §Method makes a re-sweep cheap if a future session ever actually needs the file-by-file hit list. §7 of the proposal rewritten in place from "Open questions for Johan" to "Decisions (resolved 2026-05-13)" — preserves the audit trail rather than deleting the section. Status frontmatter flipped `draft` → `accepted`. The mechanical change this commits the project to: the next research-style task session creates its scratchpad at `_generated/research/<topic>/`, not in `projects/<topic>/`. No tooling to build.

- **Second research sweep of the day — "Tolstoyism" and "Christian anarchism" in the Jubilee Edition.** First actual application of the byproduct-capture proposal accepted earlier the same day. Working zone at `_generated/research/tolstoyanism-christian-anarchism/` (gitignored — `hits.md`, `method.md`, `negative-results.md`, `open-questions.md`, `recommendations.md`, the 9 raw `.txt` extracts under `extracts/`, the 7 PSS page scans at 220 dpi under `extracts/pss-pages/`, a copy of the extractor at `scripts/extract_tei.py`, and the draft `index.md`). Polished output promoted to `docs/research/tolstoyanism-christian-anarchism/` (`index.md`, the extractor at the topic root, and the same `.txt` extracts and page scans under `extracts/`). The convention worked as written — no rescue work; every byproduct landed where the proposal predicted.

- **The sweep itself.** Two Russian root families (`толстовств` + `толстовц` for Tolstoyism, returning 43 unique TEI files; `анархизм` + `анархист` + `анархия` declensions for anarchism, returning ~110), plus the compound phrase `христианский анарх*` (3 files), plus a confirmation sweep for "so-called" / "they-call-me" verbal frames (zero hits) and the unique-attestation patterns `я не анархист` and `никакого толстовств` (one file each, both finalists). Result: three primary-voice quotes that form the spine of the survey. The 1897-12-02 diary entry to Dušan Makovický («Никакого толстовства и моего учения не было и нет», PSS Tom 53 pp. 167–169) is the only verbatim self-rejection of "Tolstoyism" in the corpus. The 1900-08-01 letter to Paul Eltzbacher («я не анархист в смысле политического реформатора […] то учение […] не политическое, а религиозное учение», Tom 72 pp. 424–426) is the only verbatim self-rejection of "anarchist." The 1901-07-28 letter to Gabriel Sacy in Cairo («Имея много общего с христианским анархизмом», Tom 73 pp. 109–110) is the only place in the entire 90-volume Jubilee Edition where Tolstoy uses the phrase «христианский анархизм» in his own voice. All three cross-checked against the printed PSS at 220 dpi — the rendered scans match the TEI extracts modulo OCR soft-hyphen artefacts. The double move is the characteristic Tolstoy one: refuse the personal label («толстовство»), refuse the political label («анархист в смысле политического реформатора»), affirm the substance under the name «христианский анархизм» when treating it as a doctrinal coordinate.

- **What the convention added over the 2026-05-10 session.** The copyright-renunciation sweep lost the file-by-file hit list per keyword, the raw grep counts, and the Johan-facing scratchpad. This session preserved all three. `hits.md` (138 lines) names every one of the ~150 unique TEI files matched, organised by keyword family and addressee class (foreign Christian-anarchist correspondence; Russian correspondence; state-power addressees; polemical works). `method.md` records the exact grep commands and the printed-page → PDF-page offset method (the offset is volume-specific because the local PDFs prepend an auto-generated cover page from the *Vseh Tolstoy v odin klik* digitisation project). `recommendations.md` is the editorial-reasoning piece that almost got lost last time, with finalist rankings, draft narrative arc, and four editorial calls deferred to Johan's judgment (translation finalisation against R. F. Christian's 1985 English edition, the Stakhovich "ridicule of Tolstoyism" callout, the Chertkov 1905 booklet *О христианском анархизме*, and the Bishop Germogen unsent letter of 1908-09-13). The polished `index.md` stays in the architecture-review register and points at the working zone for any future reader who wants to re-sweep.

- **Follow-on flagged: the genealogy of «христианский анархизм».** Johan's question after reading the polished essay: was "Christian anarchist" a term used during LT's lifetime, or is it later? Short answer from this session's evidence: yes during his lifetime, but as a term-of-art among his correspondents (Schmitt, Kenworthy, Chertkov, Ortt, Crosby) rather than a generalised category. LT himself uses it once in 1901; Chertkov gives it programmatic Russian form in 1905; Tucker's 1908 English translation of Eltzbacher still uses plain "anarchism," not "Christian anarchism." The full genealogy — first attested use in Russian and in English, when the phrase stabilised as a doxographic category in 20th-century political theory — is its own research session. Handoff at `_generated/sessions/2026-05-13-christian-anarchism-genealogy-handoff.md`.

- **Annotations pass on the tolstoyanism essay — two `extract_tei.py` bugs fixed.** Johan annotated the polished `docs/research/tolstoyanism-christian-anarchism/index.md` and flagged "encoding" issues across the v53/v72/v73 extracts plus two open questions. Two distinct extractor defects under the heading: (a) **Editorial-apparatus leak.** PSS editors' commentary («Печатается по копировальной книге №…», «Ответ на письмо…») was bleeding into the bottom of v67, v68, v72, v73, v77, v78 because the upstream TEI wraps it in `<note type="comments"><noteGrp><p>…</p></noteGrp></note>` inside `<body>`. The outer `body.iter()` was picking up the inner `<p>` because the `<note>` filter only fired when `<note>` was the current tag, not an ancestor. (b) **Whitespace before punctuation.** Inline `<title>` / `<emph>` elements whose closing punctuation lived in the tail produced patterns like «Северный Вестник .», «руководительства ,», «Лев Толстой .». The `" ".join("".join(text).split())` collapse normalised whitespace runs but didn't remove space-before-punct. Patch added an `inside_note()` ancestor check (skips any `<p>`/`<opener>`/`<closer>`/`<head>` whose ancestor chain contains `<note>` or `<noteGrp>`) and a final regex pass collapsing whitespace adjacent to `.,;:!?»` and after `«`. Applied to `docs/research/tolstoyanism-christian-anarchism/extract_tei.py` and the byte-identical sibling at `docs/research/copyright-renunciation/extract_tei.py`. All nine canonical extracts re-generated; verified by grep that no `Печатается` / `Ответ на письмо` text remains and no Cyrillic/Latin-letter-followed-by-space-followed-by-punctuation patterns survive. The print-PSS cross-check still passes for the three finalist passages. The sibling research's extracts under `docs/research/copyright-renunciation/extracts/` were *not* re-run by this session; if they share the same defects, a quick re-run there would be reasonable.

- **Open questions added: the "thirty years earlier for socialism" referent and the future letter schema.** Two of Johan's annotations weren't fixable in this session and were captured in `_generated/research/tolstoyanism-christian-anarchism/open-questions.md`. (1) **What did Tolstoy mean by "thirty years earlier for socialism"?** The 1900-08-01 Eltzbacher letter opens by praising Eltzbacher's book for doing for anarchism "what was done thirty years earlier for socialism — bringing it into the programme of the political sciences." 1900 − 30 ≈ 1870. The phrasing «программа политических наук» / «Program der politischen Wissenschaften» points to an academic-taxonomic treatment, not the polemical-revolutionary corpus of Marx and Engels. Plausible referents: the German *Kathedersozialisten* and the Verein für Sozialpolitik (founded 1872–1873) — Schmoller, Wagner, Brentano; Albert Schäffle's *Quintessenz des Sozialismus* (1875); F. A. Lange's *Die Arbeiterfrage* (1865, multiple eds. through the 1870s); Adolph Held's *Sozialismus, Sozialdemokratie und Sozialpolitik* (1877). Marx's *Capital* Vol. 1 (1867) is closer to the date but is not what Tolstoy's framing invokes. The PSS Tom 72 editorial commentary on the letter, or the second-edition (1910) Eltzbacher apparatus, may name the specific referent. Worth pinning down before quoting the line on tolstoy.life. (2) **"PSS Tom, letter id" — in any schema yet?** No. `website/src/letters/` is planned in AGENTS.md but not yet populated, and no `pssTom` / `letterId` field is defined anywhere under `website/src/_data/`. The column header in this index's letter tables is descriptive prose only. Recorded forward to the eventual letter-schema design under the obvious frontmatter shape (`pssTom: 72`, `letterId: 341`, `letterDate: 1900-08-01`, `addressee: "[[Paul Eltzbacher]]"`). Also flagged in open-questions: bare footnote-reference digits ("Ihres Buches 1") still render as a stray numeral with leading space — low-priority follow-on patch — and the upstream tolstoydigital-TEI filenames are garbled (homograph-style ц→c, х→x; «Eltzhacher» typo for «Eltzbacher»; «GabrielyuSasiGabrielSacy» for «Gabriel Sacy») and would benefit from a stable local renaming policy before `website/src/letters/` is built.

- **Annotations pass round two — footnote anchors now superscript, opener date glue repaired.** Johan reviewed the rendered extracts via the local docs server (`http://localhost:7866/research/tolstoyanism-christian-anarchism/`) and pointed out three remaining defects after the first pass: (a) **bare footnote-anchor digits** floating between spaces in the prose — "Ihres Buches 1", "religiöse Lehre ist? 2", "тебе, 2 как христианину", "ridicul'a 4 толстовства", and so on — the TEI corpus marks footnote anchors with two inconsistent tags, `<hi>N</hi>` (Eltzbacher, Sacy) and `<ref target="#noteN">N</ref>` (Schmitt, Stakhovich, Germogen, the 1894 diary), and the extractor was passing both through as plain prose. (b) **opener date glue** like "Paul Eltzbacher).1900 г. Августа 1/13. Я. П." — the `<name>` element's closing `).` directly abuts the date because TEI is logical-not-visual; the printed PSS layout uses a line-break or visual gutter that the plain-text extract can't reproduce, but the missing space-after-period made it look like a typo. (c) **the rarer TEI quirk `1901г.`** (no space before `г.`) in the v67 and v73 openers. Patch: extended the `<hi>` rule to also match `<ref target="#note*">` with digit-only content and render both as Unicode superscript (`⁰¹²³⁴⁵⁶⁷⁸⁹`, multi-digit footnotes preserved as `¹⁰`, `²¹`, etc.) bound to the preceding word with any buffered whitespace eaten before append, so the print-PSS look is reproduced exactly; added two targeted regex post-passes `\)\.(\d)` → `). \1` and `(\d{4})г\.` → `\1 г.` to repair the opener boundary collisions. Re-extracted all nine extracts; spot-checked v68_060 (the multi-digit-footnote stress case — 26 anchors numbered 1–26, all rendered cleanly), v77 and v78 (footnote-dense Russian prose), and the three finalist passages (v53/v72/v73). Patch synced into the three byte-identical copies (`docs/research/tolstoyanism-christian-anarchism/`, `docs/research/copyright-renunciation/`, and the working-zone `_generated/.../scripts/`). The bare-footnote-marker item in open-questions is now resolved — the upstream filename garble remains the only deferred item.

- **Third research sweep — "Christian socialism" and "Christian communism" in the Jubilee Edition.** Companion to the tolstoyanism / Christian-anarchism sweep earlier in the day; same method, same extractor, same byproduct-capture convention. Triggered by the Kata Dalström anecdote (Swedish socialist reported to have called LT a "Christian communist") and by Johan's broader question on how LT framed socialist and communist movements — especially around their adoption of violence as a means of social change. Working zone at `_generated/research/christian-communism-socialism/` (gitignored — `hits.md`, `method.md`, `negative-results.md`, `open-questions.md`, `recommendations.md`, 11 raw `.txt` extracts under `extracts/`, three PSS page scans under `extracts/pss-pages/`, copy of the extractor under `scripts/`, draft `index.md`). Polished output at `docs/research/christian-communism-socialism/` (index, extractor at the topic root, the 10 published `.txt` extracts and 5 page scans under `extracts/`).

- **The sweep and the headline negative finding.** Two Russian root families (`коммунизм` + `коммунист` + `коммуни` = 49 unique files; `социализм` + `социалист` + `социал-демократ` = 279 unique files), plus the two compound phrases. **`христианский коммунизм` returns 2 files in the corpus, both inside `<note resp="volume_editor">` editorial-commentary blocks — zero LT-voice attestations in the entire 90-volume PSS.** This is the strongest negative result of the day: the doxographic label that Dalström (and post-revolutionary Bolshevik commentary) projected onto Tolstoy is one he himself never reaches for. `христианский социализм` returns 15 files but only three are LT-voice — all in a tight 1889–1890 cluster (the 27 August 1889 diary entry on the Christian-socialist papers *Dawn* and *Nationalist*; the 15 January 1890 diary entry on the Yanzhul conversations; the 6–9 March 1890 letter to P. I. Birjukov listing the Christian-socialist papers among his English-language subscriptions). All three are bibliographic / descriptive uses; none is a self-application. After 1890 the compound phrase disappears from his vocabulary.

- **The doctrinal arc through the prophet period.** Eight finalist passages form the spine: the 1889-01-14 diary germ ("the anarchists are entirely right, only not in their violence" — provoked by a book on American socialism, eleven years before Eltzbacher); the 1889-08-27 diary's first programmatic critique of Christian socialism (programmes fine, means fall short of non-resistance); the 1894-07-23/24 Davidson letter (the programmatic statement — "socialistic, communistic and anarchistic theories serve to corroborate the Christian truth, which occupies the most prominent part"); *Rabstvo nashego vremeni* §VI "The Untenability of the Socialist Ideal" (1900 — socialism replaces capitalist coercion with new state coercion, reproducing slavery in primal form); *K politicheskim deyatelyam* (1903 — the sharpest single sentence on revolutionary-socialist *means*: "in the first place, immoral, including lies, deception, violence, murder; in the second place, and most importantly, these means in no case attain the goal"); *Konets veka* (1905 — "nothing shows so clearly the ever-growing tendency toward the enslavement of peoples as the spread and success of socialist theories"); *Zakon nasiliya i zakon ljubvi* (1908 — the synthesis: "socialist, communist, anarchist teachings are nothing other than one-sided manifestations of the violence-denying Christian consciousness in its true meaning"); and the 1910-09-07 Gandhi letter (socialism, communism, anarchism listed as symptoms of the inner contradiction of Christian civilisation — same context word as in the parallel anarchism research). Three cross-checked against the printed PSS at 220 dpi: the 1889-08-27 diary against Tom 50 vol17 p. 128; the 1905 *Konets veka* against Tom 36 vol05 p. 259; the 1908 *Zakon nasiliya* against Tom 37 vol06 p. 181. All three match the TEI extracts exactly.

- **Kata Dalström attribution unsourced from the local corpus.** The TEI is outgoing-only on Tolstoy's side; Dalström's visit and her reported characterisation of LT as a "Christian communist" would be in Goldenweiser's *Vblizi Tolstogo* (1922–1923), Makovický's *Yasnopolianskie zapiski* (1904–1910), the Swedish socialist press, or Dalström's own pamphlets — none held locally. Preserved as a Johan-reported anecdote in §1 of the polished essay with the gap explicitly acknowledged; pinned to `open-questions.md` for a future sourcing session. With this third sweep, the two essays (tolstoyanism + Christian anarchism; Christian socialism + Christian communism) close a coherent doxographic loop: the four labels most often attached to Tolstoy's late thought are now documented in his own words — or, where his words refuse to land on the compound (the «христианский коммунизм» case), documented in their structural absence.

- **Dalström sourcing resolved.** Kata Dalström, *Leo Tolstoj som kristen samhällsreformator* (Karlstad, 1908, p. 5), cited via Ben Hellman's *Hemma hos Tolstoj* (citation surfaced by Johan after the polished essay shipped). The pamphlet title is "Christian social reformer," not "Christian communist" — corrected the essay framing accordingly; in 1908 Dalström was still SAP (joined SKP in 1921), so the "Christian communist" shorthand that triggered the survey is an oral or later-transmitted simplification, not what the 1908 pamphlet itself prints. Updated `docs/research/christian-communism-socialism/index.md` §1 (replaced the Johan-reported-anecdote paragraph with the printed citation and the social-reformer-not-communist distinction), §5 (rewrote the "not findable" bullet as a visit-or-distance sub-question), §7 (added Dalström + Hellman under a new "On the Dalström attribution" subsection, removed the obsolete trailing paragraph). Mirrored to `_generated/research/`; resolved open-question §1 (partially — sub-question on visit-or-distance remains); cross-referenced from the Christian-anarchism genealogy handoff so the next genealogy session picks up the 1908 datum without re-discovering it. The 1908 Karlstad pamphlet joins the Schmitt / Kenworthy / Crosby / Davidson / Gronlund cluster as part of the foreign-language reception layer.

## 2026-05-14

- **Codified the per-session artifact policy** in AGENTS.md → "Artifact conventions". Every session that produces artifacts writes them under `_generated/<existing-subdir>/session-<topic>-YYYY-MM-DD/`. Default file format is self-contained HTML; Markdown is OK for Claude-consumable docs (handoffs, prompts, working notes) and for Obsidian/Eleventy inputs. Long-running multi-session research workspaces (e.g. `_generated/research/christian-communism-socialism/`) are grandfathered with their topic-folder shape. Every session ends with an explicit decision: dev-blog note into `website/src/posts/notes/`? reference HTML into `docs/<subdir>/`? both? neither? State the reasoning. Supersedes the prior flat-handoff convention (`_generated/sessions/YYYY-MM-DD-<topic>-handoff.md`); historical files left in place — not migrated. `docs/editorial/conventions.md:55` gained one parenthetical flagging the `tl-proofread-plan.md` example as the legacy flat layout so readers don't copy the old shape.

- **Split the wrap-up skills cleanly.** End-of-session (new, at `~/.claude/skills/end-of-session/SKILL.md`) owns the artifact-creation decision and runs once per session. End-of-day (existing) owns the TODO/memory/commit retrospective and runs once per day. End-of-day's "Tolstoy specifics" section now defers artifact discipline to end-of-session. The conceptual reason for the split is that a day may contain several sessions, each with its own artifact decision, but only one daily commit pass. The auto-memory's `feedback_session_end.md` was retitled "end-of-day ritual" and explicitly disambiguated from end-of-session; the older "Session handoffs go in _generated/sessions" memory was rewritten as "Session artifact policy" pointing at AGENTS.md as canonical.

## 2026-05-27

- **Completed the English translation of Biryukov's *Biography of L. N. Tolstoy*, Vol. III** — all 22 chapters now in `docs/research/doukhobors/biryukov-vol3/en/`. This session translated the final batch, Part I chs 2–6 (≈30.6k Russian words), closing a volume whose Russian capture and ch.18 gold-standard sample were laid down on 05-26. Pipeline per the established method: five parallel opus translators → mechanical QA (`qa.py` — RU→EN ratio, zero-Cyrillic, part-divider and block-quote-structure checks) → five *separate* read-only RU↔EN reviewers → one consolidated fix pass. Ratios 1.33–1.37× (Russian is the more compact language, so the English runs longer); zero Cyrillic leakage.
- **Why the separate review lane is non-negotiable.** It caught real defects, not nits, in three of the five chapters. ch.5 had «Ч. Н. Д.» mistranslated as *What Men Live By* — impossible (a finished 1881 tale, mismatched initials; the "article" Tolstoy was still continuing in spring 1886 is *What Then Must We Do?*); kept per Policy A as the literal initials *Ch. N. D.*, with the likely reading recorded in the notes (parallels the ch.1 «т. 11» → "vol. 11" call). ch.3 had the folk-tale title «Упустишь огонь — не погасишь» rendered with *reversed* sense ("Let the Fire Go Out…" — the proverb is about a *neglected* spark spreading); corrected to a literal rendering. Lighter fixes: ch.6's «писаной торбой» idiom carried an invented "dog"; ch.3's Rousseau «Confession» kept literal-singular inside Tolstoy's quote (a draft had pluralised it); a `[poslannichestvo]` translator-bracket removed to hold the corpus's bracket-free discipline; ch.2's two title-forms «Так что же…» / «Что же…» kept uniform and documented. All six calls logged in `en/translation-notes.md`.
- **Volume finalised.** `en/README.md` and `index.md` marked complete; the earlier Doukhobor-only extract (`extracts/biryukov-biography-doukhobors-EN.md`, the chs 18/19/21 passages) is now superseded by the full chapters and carries a banner pointing to them. The complete Vol. III is now available as a primary-source base for the planned wiki source-ingestion (Birukoff is the first source in the LLM-wiki method).

## 2026-06-08

- **The Kreutzer Sonata novel-dive shipped** (`docs/research/1887-1889-the-kreutzer-sonata/`) — the second flagship novel-dive, from the scope handoff written at the end of the Resurrection session. Ran the full novel-dive spine: light textual layer (novella + Afterword close-read of the doctrine, music, and murder nodes), the heaviest genesis layer (the «История писания» mine + the 1888–90 composition diaries + the 1887–91 letters), the nine-redaction textual history (the underground 8th redaction vs the printed 9th), and censorship-as-reception (the Dec 1889 ban under Pobedonostsev/Feoktistov, the lithograph circulation, Sofia's 13 Apr 1891 audience with Alexander III, the June 1891 vol. 13 printing). `verify_quotes` PASS 37/37; a separate opus verifier returned 0 blocking (2 `bans`-vocab nits, fixed). Parent `d104a629` + pointer `d8b5ce99`; submodule note `70703a7`; pushed.
- **The Afterword got its own record-creating `workRecord`** (Johan's Phase-0 call), cross-linked to the novella as `relatedWorks: companion` — mirroring the PSS, which catalogues «Послесловие» as a distinct work. A reusable precedent for any work-with-companion-text dive.
- **Marquee finding: Tolstoy owns the doctrine, not its modality.** The Afterword (his own voice — «своим выводам»; to Charles Andersen «Отвечаю: да») adopts the chastity argument, including chastity within marriage, as his own — but reframes it as «идеал… а не правило» (a compass-bearing, not the murderer's rule). Reported `extends` against both the mouthpiece (Maude) and ironic-distance (Yacobi 2005) scholarly readings. Rendered exactly per the both-directions-accuracy rule — neither softened nor hardened.
- **The `character`/`group` wiki-schema gap is now hit by three dives** (Resurrection, Kreutzer, and the Hadji Murat scope). Pozdnyshev/wife/Trukhachevsky routed as `concept` stopgaps + `needsReview`; the pending schema decision is overdue and also blocks the `--novel` mode build.
- **Wrote the Hadji Murat scope handoff** (`_generated/sessions/2026-06-08-hadji-murat-dive-scope-handoff.md`) for the next novel-dive — Tom 35 corpus map verified, the source-research layer (the «Список источников») flagged as the genesis distinctive, the *What Is Art?* "betrayal-or-fulfilment" marquee question, the historical-vs-fictional character routing (smaller schema-gap blast radius), and the Lanceray illustration cycle noted for a heavy visuals sweep.

## 2026-06-09

- **The Hadji Murat novel-dive shipped** (`docs/research/1896-1904-hadji-murat/`) — the third flagship novel-dive, run in-session (accept-edits) from the 2026-06-08 scope handoff. Six parallel write-to-file subagents drove Phases 1–3 (work-text close-read, genesis-commentary mine, genesis diaries, genesis letters, a heavy visuals sweep, the scholarship web-sweep); synthesis, verification, and handoff in the main context. `dossier.yaml` carries 34 byte-verified evidence rows, 18 entities, 12 visuals, a 5-entry scholarship triangulation, and a record-CREATING `workRecord`. `verify_quotes` PASS 34/34; a separate opus verifier returned CLEAN (0 blocking, 5 minor — the «два полюса» Shulgin-provenance flag + a doubled-Tom citation fixed, 3 cosmetic left). Committed, **not pushed** (push is Johan's).
- **The heaviest genesis layer of any Tolstoy fiction — because it has a source-research strand.** Beyond the thistle seed (18–19 Jul 1896) and the shame thread («баловство», «совестно», «глупого Хаджи Мурата» — the apparatus uses these, NOT «озорство» as the handoff guessed), the dive maps Tolstoy's archive network: Stasov at the Public Library, Grand Duke Nikolai Mikhailovich for the imperial rescripts, Bartenev, and the Tiflis chain (Esadze hand-copied 151 docs) + the Korganov eyewitnesses. The «Список источников» runs to 82 items; the manuscripts to 2166 sheets; ten redactions across 1896→1904.
- **Marquee finding: the contradiction was lived, not reconciled.** Against the three scholarly camps — fulfilment of *What Is Art?* (Simmons 1968), violation/silence (Herman 2005), private relapse (Wilson) — the diaries show Tolstoy never argued the work satisfied his theory; he felt writing it as ascetic shame and refused to publish, yet the text's withholding of all moralizing is the closest it comes to obeying the treatise's own attack on didacticism. Reported `extends`. The Nicholas ‖ Shamil mirror is Tolstoy's own stated design (`confirms`); the "anti-imperialist" label stays the mainstream's (`complicates`, per Bojanowska/Gould/Kokobobo). Rendered per both-directions-accuracy — neither softened to a clean anti-war tract nor hardened into hypocrisy.
- **The `character`/`group` schema gap is RESOLVED, and this dive is the first to apply it natively.** `website/schema/wiki-schema.md` v1.4 (dated today) added the `character` type (with a structured `prototypes[]` fiction→life edge) and `group` type; the proposal doc's gate-1 was closed and the Resurrection + Kreutzer dossiers re-routed off the `concept` stopgap. Hadji Murat → `character` (+ prototype = the historical figure); Nicholas I / Shamil / Vorontsov / Loris-Melikov → `person`; Butler (prototype F. F. Kutler) + Marya Dmitrievna → `character`; the Caucasus highlanders → `group`. The genuine routing judgment calls are logged in the dossier `needsReview` for the ingestion step.
- **Posthumous censorship, not a living-author ban.** First published 1912 in two simultaneous editions: the censored Moscow «Посмертные художественные произведения» (Belgard cut the Nicholas-I chapter from >10pp to 4½pp and excised the aul-burning of ch. XVII) and Chertkov's uncensored Berlin (Ladyzhnikov) edition that restored everything — recorded in the `workRecord` as a `passages-cut`, imperial-state ban dated 1912.
- **Built the `corpus-dive --novel` mode** — closing gate 2 (resolved with Johan: a **MODE** of `corpus-dive`, not a standalone skill — one maintained pipeline, shared gates). The depth lives in a new lean overlay `.claude/skills/corpus-dive/references/novel-mode.md`, loaded only when `--novel` is set; `SKILL.md` gained the flag + triggers, a concise *Novel mode* section, a Phase-2 breadcrumb where the base chapter-by-chapter read is overridden, and a fix to the stale "9 wiki types" line (→ 12, incl. `character`+`group`). The overlay codifies what the three hand-run novel-dives shared (lighter close-read, heaviest genesis & reception, Characters & prototypes + promoted Themes + marquee-as-hypothesis, heavy visuals, six flex points) and points at the three reference dives + the proposal. Committed `f2736d03`; pushed. The proposal doc (`docs/research/1889-1899-resurrection/novel-dive-proposal.md`) banner flipped to **BUILT**.
- **Verified by a paper dry-run, not a live test-dive.** A real corpus-dive is multi-hour, so the three completed dives are the de-facto evals; spinning up fresh test-dives isn't practical. Instead one opus subagent read the updated skill + overlay and produced a *plan + instruction audit* for *The Cossacks* — chosen deliberately as an off-distribution case (an early 1853–63 work, legally published and uncensored, with **no `works/` record**). It confirmed the spine is followable and the two load-bearing instructions (don't title-grep; replace the chapter-march) anticipated real hazards — and surfaced three places the mode's late-work calibration leaks, all hardened the same session: an absent `works/` record now routes to a record-*creating* proposal (fixed in the base work-subject Phase 0 too, since it affects all work-dives); a fourth early-fiction reception shape («acclaimed legal publication, no ban → literary-critical», `bans: []`); and marquee framing that admits interpretive/developmental claims, not only causal-contested ones. The dry-run artifact is local-only (`_generated/corpus-dive-dryrun/`, gitignored). **The next real `--novel` dive remains the true test** — a paper plan can't catch everything a live run would.
- **The Father Sergius novel-dive shipped** (`docs/research/1890-1898-father-sergius/`) — the fourth flagship novel-dive and the **first live run of the codified `--novel` mode** (the three before it derived the mode by hand). The mode held: the overlay never left the run guessing and never contradicted the spine, so **no overlay change was required**. `verify_quotes` PASS; a separate opus verifier returned clean. Parent `889496cc`; submodule note `2176122`; pushed.
- **Marquee = `confirms` + `extends`: the subject is слава людская, not lust.** Tolstoy says so himself to Chertkov (16 Feb 1891 — the carnal struggle is «одна ступень», the real one is «с славой людской», `v87_280_a16`), and the novella names it in Sergius's ch. VII prayer, which does not save him. `confirms` because the pride reading is the *received* English-language reading (not a contested minority one); `extends` because the dive grounds it in the correspondence *while composing* and sharpens the cure — not a sterner asceticism but anonymity (Pashenka's obscure service). The protagonist's vainglory has an **author-stated** prototype (A. M. Kuzminsky + vice-governor L. D. Urusov, diary 31 Oct 1890); the early redaction was a murder story (the axe-killing of the merchant's daughter), cut in the 1898 reworking (variant №7). Finished 1898 «в печать для духоборов»; never printed in his lifetime; Moscow 1911 cut the Nicholas-I material, Berlin 1912 restored it — the same posthumous shape as Hadji Murat.
- **Recorded the run in the `--novel` overlay** (optional polish, Johan's call — not a fix). Added Father Sergius to *Reference implementations* as the mode's first *validation* run (distinct from the three that derived it), and spelled out the **person-in-an-otherwise-fictional-cast** middle case in *Characters & prototypes*: a single real figure appearing as himself inside an otherwise wholly-fictional cast routes `person`, not `character` (Nicholas I), while the invented leads (Kasatsky/Sergius, Mary, Pashenka) stay `character`.

## 2026-06-10

- **The Power of Darkness novel-dive shipped** (`docs/research/1886-the-power-of-darkness/`) — the **7th corpus dive, 4th `--novel` live run, and the first drama**. Run in-session (accept-edits) from Johan's pick — chosen knowingly over *The Forged Coupon* / *The Fruits of Enlightenment* / *The Living Corpse* after I flagged that its TODO-queue position was a weak reason (the last survivor of a flat 2026-06-08 enumeration of Prophet-period fiction gaps, not a deliberate choice). Three parallel write-to-file subagents drove the 1886 witness sweep (letters/diaries), a heavy four-channel visuals sweep (15 PD images), and the scholarship/reception sweep; the full play close-read, the 31-page genesis-commentary mine, synthesis and verification ran in the main context. `verify_quotes` PASS 34/34; a separate opus verifier returned CLEAN (0 blocking, 6 minor — all fixed). Committed, **not pushed** (push is Johan's).
- **The `--novel` mode flexed cleanly to a drama.** `--novel`'s defining move — the *light* close-read, because a 300–500pp novel can't be read whole — misfires on a ~120pp play, so the flex was to **read the whole play in full, act by act** (the base work-dive textual layer) while keeping every other novel emphasis: heaviest genesis & reception, a Characters & prototypes section with `prototypes[]`, a promoted Themes section, the marquee-as-hypothesis, and heavy visuals. No overlay change was needed. A precedent for the remaining Prophet-period dramas (*Fruits of Enlightenment*, *The Living Corpse*).
- **Marquee = `confirms` + `extends` + a strong `complicates`.** The chain-of-sin doctrine («коготок увяз, всей птичке пропасть») and redemption-through-public-confession (not the naturalist horror) are the unambiguous spine — Akim warns Nikita with the proverb in Act 1, Nikita names it back as fulfilled at his Act-5 confession, and Akim blesses him «Себя не пожалел, Он тебя пожалеет»; the 27 Jan 1887 court audience read it exactly there (`confirms`). The 1886-practice → 1898-*What Is Art?*-theory link is the dive's own synthesis (`extends`, lightly backed by Sizova 2023). But **"art for the people" `complicates` in Tolstoy's own words**: the forty Yasnaya Polyana peasants who first heard it «не была понята», and Chertkov/Stakhovich then weaponised that failure to push the play past the censor as "above the masses" — while by January 1887 Tolstoy was telling A. A. Tolstaya it was written «для «большого света», для которого я писал ее». Folk form, "great world" target; by March, «моей несчастной драмы».
- **The censorship history is a reversal, and the print escaped where the stage did not.** Alexander III praised the play at the 27 Jan 1887 court reading («Чудная вещь») and ordered it staged; Pobedonostsev's 18 Feb letter («Едва ли сам Золя дошел до такой степени грубого реализма») reversed him the next day. The **stage** was banned 1887–1895 (lifted by Nicholas II, 15 Sep 1895 OS), but the **print** was permitted 13 Jan 1887 — only the Matthew 5:28–29 epigraph cut — and sold 100,000+ cheap Posrednik copies in 1887. World première abroad: André Antoine's Théâtre Libre, Paris, 10 Feb 1888, four years before Russia. Title from Luke 22:53; copyright renounced 23 Feb 1887. The `workRecord` is a FILL of the existing draft stub with two corrections (`publishedDuringLifetime` + `publishedInRussiaDuringLifetime` → true).
- **Surfaced a schema-vocab gap (flagged, not changed).** The works-schema `bans[].scope` enum (`complete-ban` / `passages-cut` / `serialization-refused` / `confiscation` / `pre-publication-rejected`) has no value for a **stage-only ban while print is free** — the exact shape of this work. Both ban entries are mapped to `complete-ban` with the nuance in the field note, and the gap is logged in the dossier `needsReview` for Johan's call (a schema change needs his nod). The VIS-08 Serov "Stakhovich" portrait turned out to be the **wrong Stakhovich** (the MAT figure Aleksei A., not the 1830–1913 reader Aleksandr A.) — `usable:false`. **Prophet-period major fiction is now essentially exhausted by dives** (Ivan Ilyich, Kreutzer, Resurrection, The Devil, Father Sergius, Master and Man, Hadji Murat, Power of Darkness); what remains in the period is the two other dramas, *The Forged Coupon* (1904), the folk-tale cycle, and *After the Ball*.
- **Graphify evaluation spike** (`_generated/research/session-graphify-spike-2026-06-10/index.html`) — executed the approved plan to test whether [Graphify](https://github.com/safishamsi/graphify) (v8, local `~/Projects/graphify-8`) can auto-generate a knowledge graph + Obsidian vault from the corpus. Ran in an **isolated copy** at `/Volumes/Graugear/Tolstoy-Graphify-Spike/` (one-way rsync of `docs/research/` **md+yaml only** — 281 files / 9.6 MB; the 42 MB of raw Russian PSS extracts, 260 MB visuals, and HTML dupes deliberately excluded after Johan's call, which also kept the run to ~1.0–1.1M input tokens). **Verdict: keep the Opus-generated vault as a complementary navigation/discovery aid**, not a typed entity store — its cross-dive "surprising connections" (e.g. *Kreutzer*'s "chastity as ideal" ⟷ *Power of Darkness*'s "chain of sin"), named community clusters, and god-node centrality add a layer the linear dives lack, but flat `concept/document` typing + partial cross-dive dedup keep it firmly complementary to LightRAG and the curated person/work/place vault. **Opus vs Haiku**: Opus cleaner/consolidated (299 nodes, single canonical Tolstoy node, ~17 min); Haiku 2× faster but noisier (506 nodes, author split into 3, ~8.5 min). Both **$0 marginal** via the `claude-cli` (subscription) backend, not the metered API. Workspace + both vaults retained for evaluation; nothing pushed.
- **The Ollama leg exposed a broken local-inference stack — now fixed.** Run C never produced a graph: the Homebrew **formula** `ollama` ships **no `llama-server` runner** (every generate 500s) — and this is the same stack LightRAG's local inference depends on (TODO #1), so it was likely silently dead. Fixed by installing the **cask** `ollama-app` (the official app bundles the runner). Even fixed, the leg is infeasible on the base M4 (24 GB): graphify can't set num_ctx through Ollama's OpenAI endpoint (needs server-side `OLLAMA_CONTEXT_LENGTH=32768`), and the only model that holds the strict-JSON schema at that context (qwen2.5:14b) loads at ~15 GB and **swaps**. So local extraction here is a hardware no; `claude-cli` is the practical backend. Reusable gotchas captured in memory (`reference_ollama_macos_llama_server`). **Follow-up for TODO #1:** verify LightRAG's local qwen2.5:7b path runs end-to-end now that the runner is restored.

## 2026-06-11

- **Tolstoy Lab Phase 1 ("stand it up") executed** — the approved graph-populated-research-site plan (`~/.claude/plans/abundant-launching-fox.md`) moved from paper to a standing repo at `/Volumes/Graugear/Tolstoy-Lab/` (own git repo, **no remote by design**). Eleventy machinery copied from `website/` and trimmed of production content (~25 MB of post images dropped); the site builds green and renders with a placeholder homepage stating the "generated, not curated" rule. The full graph pipeline then ran end-to-end from inside the session: sync → `graphify extract` → `label` → `export wiki`/`html` = **696 nodes / 946 edges / 56 communities / 66 topic-hub articles** from 304 corpus files, ~19 min cold, 2.5M input tokens, **$0** (claude-cli/Opus). Lab commits `a35b7d6` + the sync follow-up. Handoff for the Phase-2 session (the `build_pages.py` generator): `_generated/research/session-tolstoy-lab-phase1-2026-06-11/handoff.md` (local-only, `_generated/` is gitignored).
- **Two reusable findings.** (1) Graphify falls back to `.gitignore` when no `.graphifyignore` exists — and the Lab gitignores its corpus copy `_raw/`, so the scanner found **zero** files until a `.graphifyignore` was added at the Lab root (now documented as load-bearing in the Lab README). (2) Graphify's claude-cli backend (nested `claude -p`) **works fine from inside a Claude session** as a background task — the corpus-dive "can't spawn `claude -p`" limitation does not generalise to graphify.
- **Sync rule settled with Johan: only shipped dives feed the Lab.** `_generator/sync.sh` takes its file list from `git ls-files` (md+yaml only), so uncommitted/in-progress dives are excluded automatically. The session's live test case: `1903-folk-tales` looked untracked at session start but was committed at 21:26 by the parallel dive session — under the new rule it qualifies on merit, and tomorrow's in-progress dive won't.
- **TODO updated** — new priority 9 (Tolstoy Lab, Phase 1 done / Phase 2–3 queued); timelinegraph (priority 6) note that its ~200-node density gate is now met (442 entity nodes) and `/graph/` in the Lab is its reserved slot.

## 2026-06-12

- **Tolstoy Lab Phase 2 ("the generator") executed** — `_generator/build_pages.py` (~750 lines) now merges `graphify-out/graph.json` with the dive dossiers and writes the whole generated site: **523 entity pages, 22 works records, 60 topic hubs, 36 dives rendered in-site, 39 open threads**, plus all six plan additions (match report, generated-badge on every generated page, provenance footers, `/threads/`, `/graph/` atlas iframe, colophon with run-over-run deltas via `src/_data/labStats.json`). It reuses `docs/research/lib/build_evidence_index.py` by import, pointed at the Lab's `_raw/` so the shipped-dives-only sync rule carries through; entity matching is conservative (exact / name-variant / paren-stripped; surname hits are *reported as candidates*, never auto-merged). Lab validator clean on 545 files; Eleventy green (663 files); **0 dead wikilinks**. Lab commits `202e5e9` (machinery) + `ac4d627` (first generated site). Dev server: `npm run dev:preview` → localhost:8082.
- **`regenerate.sh` has now run end-to-end as one command — in 54 seconds.** The handoff's open question is answered: graphify's content-keyed cache works across `_raw/` wipes (first re-run: 276/304 semantic-cache hits, only the genuinely-new late-voice/folk-tale-era files re-extracted; second run all-cached, $0). The graph stands at **745 nodes / 1010 edges / 61 communities**. One real failure surfaced and fixed en route: dive pages reference `visuals/` images the md+yaml sync deliberately excludes, and Eleventy's image transform treats a missing `<img>` as **fatal** — fixed twice over (generator rewrites relative images to an explicit "not synced" note; `failOnError: false` belt-and-braces in the Lab config).
- **Two schema/typing findings.** (1) The works-routing needed a **Lab-only 13th wiki type `work`** (validator copy flexed per the plan's pre-approval): a corpus work *without* a dived works record (e.g. the folk tales, Four Gospels) keeps its entity page and inbound links instead of falling into a dead end. (2) `build_evidence_index.py`'s `WIKI_TYPES` set is **stale at 9 types** (pre-v1.4, missing `edition`/`character`/`group`) — in the Lab run it briefly mistyped characters as `work`, and in production it makes the evidence-index lint misflag those entities under `worksRoutedNotWiki`. Flagged as a separate one-line fix + index regeneration (task chip spawned).
- **The three verification threads pass through the generated site**: the *What Is Art?* lived contradiction leads the Hadji Murat record (dive question + graph link both render); chastity-as-ideal has its own cross-dive entity page («Chastity doctrine (ideal not rule)») linking Kreutzer and Power-of-Darkness contexts; the Doukhobor cluster aggregates Father Sergius / The Devil / Resurrection / Master and Man quotes on one page fed by four dives. Phase 3 (the side-by-side evaluation of ~3 dual-existence entities) is the remaining checkbox.

## 2026-06-30

- **Interactive reader's-edition pilot ran end-to-end on The Great Sin (English leg) — and the read-along is proven.** Authored the faithful 1905 *A Great Iniquity* (Mayo/Tchertkoff) edition (10 sections, Introduction + Parts I–IX) from the audiobook's `chapters/` (the *faithful* text — `chapters_flow/` is audio-punctuation-altered), then ran the whole chain: `reader.segment` (336 sentences) → audiobook `build_audiobook.py` over the SEG_JSON contract (bm_daniel, 346 clips, ~54 min) → `reader.build_epub`. **Read-along confirmed working in Thorium Reader** — our recorded voice + light-yellow sentence highlight, in sync. Bundle lives at `docs/reader/non-fiction/essays-and-criticism/the-great-sin/` (regenerable `build/` gitignored).
- **Decision: drop Apple Books as the read-along target; keep editions reflowable.** Apple Books renders EPUB media-overlay read-along **only for fixed-layout books** (verbatim in Apple's Books Asset Guide: "read aloud content is supported only in fixed-layout books"), corroborated across multiple sources and the DAISY support grid. A reflowable prose edition gets no narration control there, on Mac or iOS, by design — *not* a defect in our file (EPUBCheck is clean). Fixed-layout would sacrifice resizable type and accessibility, wrong for a reading edition, so it's deliberately out of scope. The read-along surfaces are **Thorium / Readium / Kobo and the web edition**. Recorded as a reference memory + corrected in the workflow-design spec's verification section. The pilot's whole job was to surface exactly this kind of dead end before every future work chased it.
- **The pilot shook out three real `build_epub.py` bugs the 32 green tests never caught** (EPUB conformance, not unit logic): the SMIL `<text src>` lacked the `../text/` prefix so it pointed at a nonexistent file (broke the text↔audio tie — the original "no control" symptom); `dc:contributor role="trl"` is invalid in EPUB3 (needs the `marc:relators` refines pattern); and the highlight CSS never reached the document because ebooklib strips inline `<link>`s (must register via its API). All fixed — the EPUB is now **EPUBCheck 3.3 clean (0/0/0/0)** with three new guard tests (34 total), and the missing `reader.build_epub` CLI (`main()`) + a per-work `meta.json` were added. Found EPUBCheck absent and installed it (`brew install epubcheck`); it's now the structural gate.
- **Still to do for The Great Sin:** the Russian spine `the-great-sin.ru.md` (real hand-work — Tolstoy's paragraphs and the translation's don't break 1:1, so paragraph-parallel alignment needs editorial judgment), the machine-translation leg, and **Phase 3** (reconcile reading reflections against the existing dive → overview page + entity steer). Then *A Confession* as the first full from-scratch loop.

## 2026-07-01

- **The Great Sin — Russian spine authored and aligned.** `the-great-sin.ru.md` (PSS «Великий грех», t. 36, pp. 206–230) now sits paragraph-for-paragraph beside the 1905 English, so the two share one **paragraph coordinate** (paragraph N in either version = the same place in the work). Both segment to **10 sections / 135 paragraphs** and pass `reader.segment --spine-json`. The Russian wording is untouched — only paragraph *breaks* were moved (splits/merges at real sentence boundaries) to fit the English, which is the incumbent because its read-along audio is already aligned to it. Method is deterministic and recorded in the bundle's new `alignment-notes.md`.
- **The alignment surfaced 3 defects in the pilot English edition** — a single sentence split across a paragraph break in three places (Part II ×2, Part IX ×1: e.g. "…he comes in" / "contact."). These read and speak wrong and blocked clean alignment (matching them would need equally broken Russian breaks). With Johan's go-ahead, **repaired the English** (rejoined the three) rather than propagate the breakage. **Deferred consequence:** the repairs shift sentence boundaries in Part II & IX, so the English audio + EPUB for those two chapters is now stale and must be regenerated at the next audio pass (separate `projects/audiobook` repo). `build/` is regenerable/gitignored; the current build keeps working until then. New TODO item logged.
- **Spine cleanups:** stripped one stray PSS footnote marker (³⁷) at the Henry George citation; fixed 5 OCR homoglyphs (Latin letters inside Cyrillic words — «зa»→«за» ×3, «тело to человека», «Ha-днях»); added the closing dateline «Ясная Поляна, июль 1905 г.» to parallel the English (editorial — PSS omits it; manuscript signed «Июль 1905» per the commentary). The Russian is machine-extracted and not yet fully proofread against the scan — flagged for a later reading pass. Committed, not pushed.
- **Later that day — closed the three open Great Sin tweaks.**
  - **Dateline resolved.** Reverted the first-pass «Ясная Поляна, июль 1905 г.» to the attested manuscript form **«Июль 1905»** (no place). Every other spine fix corrects transcription without adding content the manuscript lacks; inventing a place name to mirror the English would have been the one exception. Recorded in `alignment-notes.md`.
  - **RU spine proofread — clean.** Checked `the-great-sin.ru.md` paragraph-by-paragraph against the printed scan (PSS Tom 36 = local `vol05` — the `jubilee-edition/volNN` dir names are publication-order, not Tom number; the resolver already exists at `docs/research/pss-volume-mapping.md`, don't re-scan). Full read + word-level diff against the scan's own independent OCR, every conflict adjudicated on the page image. No wording errors beyond the already-recorded fixes; all names/numbers verified. A *second* real PSS editorial footnote exists in Part II (¹, «Речи и статьи Генри Джорджа») but never entered the extract, so nothing to strip. The "not fully proofread" caveat in `alignment-notes.md` is now updated to record the pass.
  - **English audio regenerated (Part II & IX).** Resynthesized the ~30 clips the 3 rejoins touched (cache reused the rest), rebuilt all 10 section `.m4a` + `timing.en-1905.json` (**343 clips**, was 346) + the read-along EPUB (EPUBCheck-clean). Two latent traps found and fixed along the way: `build/segments.en-1905.json` was stale from before the rejoins (so the `--spine-json` alignment check had been validating against pre-repair paragraph counts), and the `projects/audiobook` README + docstring still described the retired pipeline (a `--dry` flag that silently does nothing, a `.m4b` the build no longer makes, and `chapters_flow/` which the build stopped reading when it moved to `segments.json`). Corrected all of it. **Still stale:** the standalone `.m4b` download — the current build deliberately doesn't produce one; revive only if a plain audiobook file is still wanted. All commits local, not pushed (audio repo commits: chapter rejoin, README fix, docstring fix).

## 2026-07-03

- **Reader v1 rounded out (morning session).** The work overview page is now the bundle's front page (bare folder URL serves it), the reader top bar became a breadcrumb (Library › work title), and the Library got its own aligned top bar. The RU edition titles itself «Великий грех» via `meta.ru.json`. Four commits, pushed. Details in the session handoff; decisions recorded there: bundle layout stays flat, `reader/` stays in the URL until the Eleventy publish step decides the public shape.
- **Reader polish from first real reading use (evening session).** Four tweaks straight from Johan's reading experience: spacebar toggles the narration; default line length 66→56 ch; the read-along highlight dropped its yellow for a subtle theme-aware wash (page background mixed toward the ink — darkens on paper/sepia, lightens on dark); notes can now be edited in place (W3C `modified` stamp). Commit `78eb1022`.
- **First voice-notes-driven audio repair pass — the annotate-while-listening loop works.** Johan's 5 in-reader voice notes (exported as W3C annotations) became three *engine* rules in `reader/speech.py` + `segment.py`, so they hold for every future edition: (1) `…?" I asked.` stays one sentence — split off, a 2-word clip can't land a falling cadence; the rule fixed all **5** occurrences, not just the 2 flagged; (2) money is spelled out (`$1.40` → "one dollar forty") — restoring a rule that had been silently lost when text-shaping moved from `flow_preprocess.py` to `speech.py`; (3) the Part I professions list gets a full stop per item (the one pause Kokoro honors). 9 clips resynthesized, all 10 sections re-mastered (timing 338 clips, was 343 — the five merges), EPUB rebuilt EPUBCheck-clean workflow intact, 12+39 tests green. Commits `5a9ee30b` + `4376fdfe`, local.
- **Pronunciation has a hard ceiling — and a cheap probe.** "Kvas" can't be spoken /kvɑːs/ by this stack: English phonotactics has no initial kv-cluster, so the g2p letter-names the K (any kv-spelling → "kay-v…"), the phonemizer layer mangles espeak's raw-phoneme notation, and the CLI exposes no phoneme input. Settled on `quahss` → /kwɑːs/ (Johan OK'd — the natural anglicization). The reusable technique: `espeak-ng -q --ipa` screens respelling candidates in seconds, no ears needed. Recorded as finding 16 in `docs/audiobook-pipeline.md`.

## 2026-07-04

- **The Great Sin — second read-through fixes (morning).** A finer line-by-line read+listen of the finished edition. Text fixes (`bytheir`→`by their`, `Alexander II.`→`Alexander II`) and a few speech rules (Alexander II → "Alexander the Second", the Part IV policy list full-stop-flattened like Part I) landed in `reader/speech.py`; the interpretive digs went to the dive-steer file `docs/research/1905-the-great-sin/annotations.md` (+84 lines — the English-title reframing, use-right principle, translation-diagnostic phrases). Commit `096fa0da`, pushed.
- **The Great Sin — the audio-only sentence merge (`speechGroup`), plus the last short-phrase pitch fixes.** The deferred build session. Kokoro rises on a short clip synthesized alone; the fix is to feed it more context by gluing the short sentence to its neighbour. Built `merge_speech_groups` in `reader/segment.py`, driven by an **explicit id list** (`speech.py MERGE_FORWARD`) so it can't misfire on legitimate rising *questions*. The key architectural point: doing the merge **before** `segments.json` keeps the read-along contract (1 sentence = 1 SMIL `<par>`) intact, so **no** audio- or EPUB-builder changes — the old build-level `merge_short` was removed precisely because merging *after* the fact broke that mapping. Scope shrank on inspection: of ~6 flagged spots only *one* was a real merge ("But we are wrong."); two were ordinary `speech.py` tweaks (welfare list flatten, "God," pause); and Johan's correction on "…their parasites." (which I'd wrongly filed as a limit) was fixed with a comma→em-dash in speech. Both prosody questions — the rising "parasites" and Johan's "make commas slightly longer" — were settled by **measurement**, not ear-guessing: installed `praat-parselmouth`, which showed the em-dash drops the tag 149→111 Hz *and* that **comma pause length is a fixed Kokoro constant (~140 ms) with no lever** (speed stretches words, semicolons ≈ commas). So "longer commas by default" is not reachable; baseline accepted. Section headings and "Why is this?" documented as accepted Kokoro limits in the bundle's `alignment-notes.md`. 45 tests green. Commit `9d5da688`, pushed. Findings #12 (rewritten) + #17 in `docs/audiobook-pipeline.md`.

## 2026-07-05

- **The Great Sin — Phase 3 overview page (the first live reader-editions Phase 3).** Settled the scope with Johan up front: Phase 3 produces the **overview page + entity steer**, not a rewrite of the academic dive (the dive stays cited substrate — this is distinct from corpus-dive's own "Phase 3"). Then narrowed further at Johan's steer — the real aim is the overview page and, above all, its **tone**: a voice in line with Tolstoy's own, found by critiquing the machine dive's register (meta-aware, academic scaffolding, hedged). Agreed a **"Blend" voice** (plain factual spine, Tolstoy's own phrases carrying the moral turns) from three sample registers, over a cooler "report plainly" and a warmer "echo his cadence".
- **Drafted `docs/reader/non-fiction/essays-and-criticism/the-great-sin/overview.md`** (replaced the scaffold), then revised it against Johan's **9 reading annotations**: dropped **"single tax"** — a value-word that files a moral argument (the *evil of landed property*) under libertarian economics, Johan's core objection to the dive; added `[[wikilink]]` marks for *1905 Revolution* + *Henry George* (the entity work-list seed); paired every Russian excerpt with an English gloss in one format; a dry factual lede that states both titles («Великий грех» / *A Great Iniquity*) up front with the reader link; dropped the author's name from the masthead; `<abbr>` on PSS; moved the «русские паразиты» self-indictment deeper (progressive deepening); removed the closing editorial sentence at Johan's request. Verified rendering via serve.py (abbr + wikilinks + glosses correct). Committed, not pushed.
- **The title-provenance archival dig was deprioritized** to a lean interpretive note (Johan reversed the earlier "full archival dig" call).
- **Three general authoring rules surfaced and were saved to memory** (they apply to all overview pages / the wiki / everywhere): **limit naming Tolstoy** (everything is his — skip the author line), **mark every abbreviation** with `<abbr>`, and the **overview-page shape** (dry lede → progressively deepen; Russian always glossed in one format; reader link up top; avoid academic value-words). See `feedback_overview_page_conventions`, `feedback_limit_naming_tolstoy`, `feedback_mark_abbreviations` in memory.
- **Still open on The Great Sin:** Johan's own annotations on the dive (the critique that further refines the overview), the entity steer, and the machine-translation leg. Handoff: `_generated/sessions/session-great-sin-phase3-2026-07-05/handoff.md`.

## 2026-07-11

- **The Great Sin re-dive — Johan's own annotations on the academic dive landed (13 notes on `index.md`).** These are the "re-dive" proper: the human close-read the machine dive couldn't do. Captured in the dive's `annotations.md` as a dated section, with two Russian translation asks resolved («Необходимый переворот» = "A Necessary Revolution"; «Мне жалко» = "I regret it"). Clarified the loop (I'd briefly muddled it): the **re-dive = Johan annotating the old dive → directs the overview page → which is the base for the final website works overview page.** The old machine dive stays **untouched** as cited substrate.
- **The throughline of his notes:** the dive's *own prose* editorialises — it imports modern political-economic labels absent from Tolstoy's text (*land monopoly*, *single tax "as its remedy"*, *Socialist-Revolutionaries*, *George-derived*, *Georgist-Tolstoyan*), filing a religious/moral argument under libertarian/economic categories, and stages a false academic debate ("does the single tax compromise his anarchism?") that misdirects from the question Johan holds central: **why is landed property a sin but not money or other private property.** Decision: **leave the old dive as-is** — these become **constraints on the re-dive, not edits to the substrate.** Fixed the one place the overview still carried it: "land monopoly" → "private property in land" (also George's own phrase). Recorded the calibration principle: *don't tell readers what Tolstoy thinks before they've read him.*
- **Explored recreating "the edition Tolstoy wanted to publish"** (Johan's idea — the fuller pre-cut text he regretted, «Мне жалко»). Feasibility checked against the corpus: **yes, as an editorial reconstruction.** The PSS apparatus (Gorbachev, t. 36 pp. 656–665) pins the Chertkov cuts as a discrete, page/line-keyed set — variants **№ 9–12** = the excised original ch 1, end of ch 2, ch 6, end of ch 7 ("excised at Chertkov's initiative and with Tolstoy's agreement") — plus «Необходимый переворот» from the intro. Russian only (the 1905 English never had these passages; an English "uncut" would be our non-authoritative translation). **But "switch to it" hits a reader limit:** `reader/segment.py` requires 1:1 structure across versions (it rejects a section-count mismatch against the spine), so a longer text can't be a synced side-by-side switch without new machinery. Options: standalone page / in-place "restored cuts" reveal / extend the reader. **Undecided.** Recommended starting with a bounded "The cut" treatment (which also serves the #5/#6 annotation asks). Nothing built.
- **A Confession — started in parallel (Stage A of the existing plan).** Ran `_generated/Fable-plan-a-confession-reader-edition.md` Stage A while Johan annotated the Great Sin dive. Fresh Russian re-extraction (`--choice=reg --notes=auto`): bibl confirms **Tom 23, pp. 1–59**; came out **byte-identical** to the dive's extract (this work has no inline-note apparatus, so the note-tail fix is moot here). Structure confirmed: **16 chapters (I–XVI)**, the 1882 dream postscript **inside ch XVI**. Both English translations found clean and **local** — **Aylmer Maude 1921** via `primary-sources/standard-ebooks/` (proofread; recommended) and **Leo Wiener 1904** ("My Confession") via the tolstoyarchive mirror. **Decision sheet:** `_generated/research/session-confession-prep-2026-07-11/decision-sheet.html`. **Gated — awaiting Johan's two picks:** translation (Maude vs Wiener — deferred) and section structure (16 vs 17 — the dream as its own unit or inside XVI). Recommend **Maude, 16**. No build started (Stage B is post-decision).

## 2026-07-13

- **corpus-dive skill now births new dives in the reorganised research tree.** Picked up the folder-move handoff: a prior session had relocated every *existing* dive into the nested tree (`docs/research/works/<genre>/<subcat>/`, `themes/`, `_meta/`) and repaired the generators, but the skill that *creates* dives still wrote to the old flat `docs/research/<slug>/`. Settled the one open design call with Johan — **a new dive mirrors the work's own page on the live site** for its genre/subcat (the site already made that editorial call; the skill's Phase-0 lookup that pins the `workRecord` path now does double duty), theme dives → `themes/`, methodology → `_meta/`. Defined `<dive-dir>` once and pointed every output path at it; slug still = folder name = `topic.slug`, only the parent changes. Also fixed the example-dive pointers the move had broken (`novel-mode.md`, the "Modeled on" line) and corrected a stale renderer note (serve.py dropped the `nl2br` extension, so the old "one source line per paragraph" rule was wrong — hard-wrapping is now fine). Commits `001e32a4`, `0e74db1e`.
- **serve.py now resolves the flat-era cross-dive links the move broke.** Measured the fallout: **542 broken `../<slug>/…` links across 51 of 53 dives** — the move changed each dive's depth *and* relocated (sometimes renamed) the targets, so the old flat links missed on both ends. Rather than churn 470 links across 53 research files, added a render-time resolver: it rewrites `../<slug>/…` to the sibling's real root-relative URL using `move-map.tsv` (old-slug → new-path) plus current folder names, so the flat `../<slug>/` authoring convention every dive and the skill already use stays valid and new dives work for free. **486 of 540 resolve** (verified against a full `serve.py --build-only`); the rest are out-of-docs `website/` note pointers and 6 links to repo-root / unmerged-branch files, left as authored. Self-check in `docs/test_dive_links.py`. Commit `72b4a9c9` (later rewritten — see below).
- **Purged 557 rights-reserved images (333 MB) from the branch history.** The move's `git add -A` had committed the git-ignored `visuals/` research cache into commit `8a5f0aa7`; a later commit un-tracked them, but the image blobs still lived in history and had already been pushed to the public remote. Rewrote the 11 commits from the move onward (`filter-branch`, `:(glob)docs/research/**/visuals/**`) so the images were never added — verified the tip tree is byte-identical (no content change, only history slimmed) and 0 `visuals/` blobs remain reachable. Johan force-pushed; confirmed origin is clean (old leaky commit `8a5f0aa7` no longer reachable, new tip `da517fed`). Local backup branch `backup/pre-image-purge` + `refs/original` kept for now. Drafted a GitHub Support note asking them to garbage-collect the now-unreachable objects (the images can still be reached by direct SHA on GitHub until it GCs) — **Johan to send.**

## 2026-07-17

*(entry written retrospectively 2026-08-09 — this session and the next never got their own LOG entries)*

- **A Confession, Stage A decided and Stage B started.** Johan chose **Wiener 1904 over the recommended Maude 1921**, and 16 sections — historical closeness won: the edition appeared in Tolstoy's lifetime and he corresponded with Wiener about it (PSS 75:193). Road prep committed for a cloud/iPad session (`872fe3af`), then the text work itself (`8bf4f930`): the Russian spine from the tracked TEI extract, and the Wiener English hand-aligned paragraph-for-paragraph at Johan's explicit request for full rigor. Three real defects found in the source e-text along the way — a ~500-word gap in ch. IV restored verbatim from Wikisource, a genuine duplicated passage in ch. V removed, and an Ecclesiastes quote in ch. VII confirmed as Wiener's *own* omission rather than a digitization fault. Also mended the 6 dangling cross-links the folder move left behind (`069b4846`).

## 2026-07-27

- **First two wiki entity pages written** (`c1f15710`) — Henry George and Isabella Fyvie Mayo, populating the shared entity layer the folder move created empty. Both carry the live wiki-schema v1.4 person shape with `liveStatus: new` and pass the validator unchanged; graduation to `website/src/wiki/` stays a move plus a review, as designed. Grounded in the Great Sin dive and Johan's reading annotations, with `NEEDS PRIMARY SOURCE` markers rather than inferred values. Resolved the Maria Tolstaya disambiguation (the existing vault page is the daughter, Maria Lvovna) and confirmed Makovitsky / Mazzini / Nikolaev / Buturlin as genuinely missing. Mayo **may** be the unidentified "I. F. M." of the 1904 *Bethink Yourselves* translation — an open question in `src/sources/log.md` since April; recorded as likely, not established.

## 2026-08-09

- **A Confession's spine alignment was quietly broken, and is now fixed** (`6d9c231b`). The July handoff recorded that all 16 chapters matched — true of the prose, false of the file. The four capture notes from that session sat inline as standalone HTML comments, and `reader.segment` splits paragraphs on blank lines, so each one counted as a paragraph; the spine check failed in chapters IV, V, VII and XI. Moved all four into a new `alignment-notes.md` — where two of them already pointed by name, and which Task B2 owed anyway. Leaving them attached to a neighbouring paragraph was not an option: `resolve_reading_text()` strips CriticMarkup and wikilinks but **not** HTML comments, so the note text would have reached both the reading display and the spoken audio. The diff is 31 deleted comment lines and nothing else — no Russian or translated text touched. Both editions now segment to 16 sections / 212 paragraphs; `reader/tests` 45 passed. Added the `meta.ru.json` / `meta.en-wiener.json` that B2 also owed.
- **The general lesson: "the chapters align" and "the segmenter agrees" are different claims.** The July session verified the first by reading and recorded it as if it were the second. Anything downstream of the paragraph coordinate — audio timing, the read-along EPUB — is built on the segmenter's view, so that is the one worth asserting. Cheap to check, and it caught four chapters.
- **Discovered the Great Sin's machine leg was never built.** `the-great-sin.en-machine.md` does not exist; it is still open on the TODO. So A Confession's B3 will be the project's **first** machine-translation leg, not the second, and whatever register it sets becomes the pattern. There is no precedent file to copy conventions from — worth knowing before someone goes looking for one.
- **Sized B3 honestly and batched it.** The Russian spine is 21,400 words / 128,000 characters — **2.5× The Great Sin**, and Cyrillic tokenizes at ~2.5 chars/token. Translation plus diagnostic is ~300–400k tokens end to end, with at least one context compaction partway. The risk is drift, not cost: a compaction mid-translation loses the register set in the early chapters, and for a text whose whole job is being a consistent fidelity ruler that is a defect. Split into four batches on the book's own turns (I–IV, V–VIII, IX–XII, XIII–XVI), committing each, with the `translation-diagnostic.md` as its own separate session since it needs both finished texts loaded at once. No English written yet — B3 starts from zero. Conventions, term glossary, and four open questions (including whether to capitalize "God" against the PSS's lowercase `бог`, which is likely a Soviet editorial artifact rather than Tolstoy's) are recorded in `_generated/sessions/session-confession-b3-2026-08-09-handoff.md`.

## 2026-08-10

- **A Confession's machine-translation leg is written — the project's first** (`a8a4573f` → `de4403e8`). Ran Task B3 as the previous session batched it: chapters I–IV, V–VIII, IX–XII, XIII–XVI, committing each, re-reading the previous batch's last chapter before starting the next. `confession.en-machine.md` now passes the spine check at 16 sections / 212 paragraphs against the Russian, chapter for chapter; `reader/tests` 45 passed. The batching held — the register set in chapter I is the register in chapter XVI, which was the whole point of splitting it.
- **Decided to capitalize "God"** against the PSS's lowercase `бог`, taking the handoff's own recommendation rather than blocking on it. The reasoning: lowercase `бог` reads as a Soviet editorial convention, not Tolstoy's manuscript, so mirroring it in English would carry an edition artefact into the translation as though it were authorial. Recorded in the bundle's `alignment-notes.md` and flagged to Johan as a one-line reversal if he disagrees. `«бог 1 и 3»` in chapter IX is rendered "God, one and three", following the guess already in the notes; still unverified against a facsimile.
- **Quoted material comes through the Russian, not the English tradition.** Socrates, Schopenhauer, Ecclesiastes, the Sakya-Muni legend and the whole liturgical stretch in chapters XIII–XV are translated from Tolstoy's Russian rather than replaced with the King James wording, Schopenhauer's German or the English service books. The edition's job is to show what the Russian reader read. That includes Solomon's "I commended mirth" saying, which Wiener's published translation leaves out.
- **Three new spine defects surfaced** (`cc9eb7bd`), all of the kind that only shows up when someone has to render every sentence: a `слитком`/`слишком` letter swap in chapter I, an unclosed `«` in chapter III, and — new this session — a stray semicolon in chapter V that splits «мы не имеем ответов» in two. None repaired in the Russian, since repairing the spine is a separate decision; all three translated as intended so the English does not carry a digitization artefact forward. Recorded in `alignment-notes.md`.
- **Still separate by design: `translation-diagnostic.md`.** It needs both finished texts loaded side by side and is comparison work, not composition; bundling it into the translation is what would push the session over the edge. One finding is already banked — Wiener renders «я очень рано стал много читать и думать» as "since I began to read philosophical works at fifteen years of age", inventing both the subject matter and an age the Russian never gives.

- **The Great Sin's machine-translation leg is done, and with it the pilot bundle's format set** (`488a00fc` + `79f56864`). Ran `_generated/Fable-plan-great-sin-machine-translation.md` top to bottom, following A Confession's leg from this morning as the pattern. At 7,774 Russian words — roughly a third of A Confession — no batching was needed and the diagnostic fitted into the same session, which was the one genuine judgment call the handoff left open. `the-great-sin.en-machine.md` segments to 10 sections / 135 paragraphs against the Russian spine, paragraph for paragraph. The bundle now carries all three layers: Russian spine, the 1905 published English, and the machine English that measures it.
- **The 1905 translation does not translate Henry George — it reprints him.** This is the fidelity report's headline finding, and it is structural rather than a slip. In sections II and IX, where Tolstoy quotes George at length in a compressed Russian paraphrase, Chertkov and Mayo went back to George's English and printed that instead. So the English reader gets George's own words — "chattel slavery", the Burlington example, the fuller manna passage, "the times are ripe for it" — while the Russian reader got Tolstoy's George, and the two are different texts. The same happens once with the Matthew epigraph, given in King James wording. It is a defensible editorial choice, and for Chertkov an obvious one, but it means the 1905 cannot answer the question the machine layer exists to answer.
- **The losses that change the argument, not just the style.** Tolstoy's own «и за 50 копеек в России» — the sentence that turns George's American wage figure into a claim about Russia — is dropped, as is the sentence giving the *cause* of low wages. «Лицемерие» is dropped from the closing triad of section V, immediately before section VI opens with "Woe unto you… hypocrites", which is what that word was there to set up. George's «духовные силы» become "mental powers", secularizing the work in an essay arguing the change can only come from religious feeling. Holdings in dessiatinas are reported as acres, understating them by about two-thirds. And "their Christian trend of character" is added to the Russian people with no source in the Russian at all.
- **Kept the report standalone rather than marking the text, as the plan required.** Highlight marks would mean editing `the-great-sin.en-1905.md`, which the read-along audio (338 clips) and the EPUB are built from; the audio cache is keyed by clip ID rather than by text, so any edit under an unchanged ID keeps stale audio silently. Whether to mark at all, where, and with what rebuild protocol is Johan's decision, to be made with the report in hand.
- **Seven more spine defects, and one of them travels.** Rendering every sentence surfaced small transmission faults in `the-great-sin.ru.md` — split words (`е сть`, `мог о бы`), a `я`/`и` swap, a wrong verb form, a stray comma, and `с телегами` where the context wants «телятами» ("calves"). None repaired; all translated as intended and recorded in `alignment-notes.md`. The interesting one is `слитком` for `слишком`, **the same typo already found in A Confession's spine** — two different volumes, same single-letter substitution, which points at a shared source or a shared OCR pass rather than coincidence. Worth remembering the next time a spine is proofread. These also sit awkwardly against the 2026-07-01 pass that reported this text clean against the scan; the likeliest explanation is that a word-level diff normalizes whitespace and so cannot see a split word.
- **Two conventions settled for machine legs generally.** The plan and A Confession disagreed on the `translator` string, and on quotation marks the plan described the 1905 file as using curly quotes when it actually uses straight ones. Both resolved towards A Confession: the machine Englishes are the set that has to read consistently with each other, and the differing quote style also makes it visible at a glance which column you are in. Capitalization of «Бог» did not arise here, and that absence is itself evidence — this spine (Tom 36) capitalizes throughout while A Confession's (Tom 23) lowercases, so the lowercasing looks like a per-volume house decision rather than anything authorial, which is what the Confession call assumed. Noted in both bundles' alignment notes.
